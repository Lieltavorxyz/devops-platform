export const categories = [
  {
    id: 'terraform',
    label: 'Terraform',
    icon: '🏗️',
    color: 'purple',
    description: 'State management, modules, drift, workspaces',
  },
  {
    id: 'kubernetes',
    label: 'Kubernetes',
    icon: '☸️',
    color: 'blue',
    description: 'Core concepts, scheduling, networking, troubleshooting',
  },
  {
    id: 'eks-aws',
    label: 'EKS & AWS',
    icon: '☁️',
    color: 'orange',
    description: 'Managed Kubernetes on AWS, networking, IAM integration',
  },
  {
    id: 'cicd-gitops',
    label: 'CI/CD & GitOps',
    icon: '🔄',
    color: 'green',
    description: 'ArgoCD, GitHub Actions, deployment strategies',
  },
  {
    id: 'architecture',
    label: 'Architecture',
    icon: '🏛️',
    color: 'teal',
    description: 'System design, multi-account strategy, platform engineering',
  },
  {
    id: 'observability',
    label: 'Observability',
    icon: '📊',
    color: 'yellow',
    description: 'Monitoring, alerting, SLOs, distributed tracing',
  },
];

// difficulty tiers: easy | normal | hard | expert

export const questions = [
  // =============================================
  // TERRAFORM (10 questions)
  // =============================================
  {
    id: 'tf-001',
    category: 'terraform',
    question: 'Your team stores Terraform state in S3. A colleague runs `terraform apply` at the same time as you and both plans succeed. What prevents state corruption, and what happens if locking is misconfigured?',
    answer: 'DynamoDB is used as a lock table for S3 backends. When you run plan or apply, Terraform acquires a lock via a DynamoDB item keyed by the state file path. If locking is misconfigured or missing, two simultaneous applies can write to the same state file, causing state corruption where resources exist but are missing from state or vice versa. You would need to manually reconcile with `terraform import` or edit the state file directly.',
    hint: 'Think about what DynamoDB does in the S3 backend configuration.',
    difficulty: 'normal',
    tags: ['state', 'locking', 's3', 'dynamodb'],
    keyPoints: [
      'DynamoDB is the lock table for S3 backends',
      'Lock is acquired during plan and apply',
      'Without locking: simultaneous applies corrupt state',
      'Recovery: terraform import or manual state edit',
    ],
  },
  {
    id: 'tf-002',
    category: 'terraform',
    question: 'Why is storing secrets in Terraform state considered a security risk, and what are two concrete mitigations?',
    answer: 'Terraform state stores all resource attributes in plaintext, including database passwords, API keys, and TLS private keys that were passed as resource arguments. Two mitigations: (1) enable server-side encryption on the S3 bucket (SSE-KMS) and restrict access via IAM policies and bucket policies, and (2) use a secrets manager (like AWS Secrets Manager or Vault) so the secret value is a reference ID in state rather than the actual secret. You can also enable S3 bucket versioning with MFA delete to prevent unauthorized state tampering.',
    hint: 'Consider what gets written to the tfstate JSON file when you create an RDS instance with a master password.',
    difficulty: 'normal',
    tags: ['state', 'security', 'secrets'],
    keyPoints: [
      'State stores all resource attributes in plaintext',
      'Mitigation 1: SSE-KMS encryption on S3 + IAM restrictions',
      'Mitigation 2: store only a reference ID, not the secret value (Secrets Manager / Vault)',
    ],
  },
  {
    id: 'tf-003',
    category: 'terraform',
    question: 'You have a Terraform repo with environments (dev, staging, prod). When would you use workspaces vs. a directory-per-environment structure, and what is the main risk of workspaces?',
    answer: 'Workspaces are useful when environments are structurally identical and only differ by variable values (instance size, replica count). Directory-per-environment is better when environments diverge in resources or you want independent state files with separate access controls. The main risk of workspaces is that all environments share the same backend configuration and state bucket, so an IAM misconfiguration could expose prod state. Additionally, running `terraform workspace select prod` followed by a careless apply in what you thought was dev is a real incident pattern.',
    hint: 'Think about state isolation and what happens when a developer forgets which workspace is active.',
    difficulty: 'hard',
    tags: ['workspaces', 'environments', 'state'],
    keyPoints: [
      'Workspaces: identical environments differing only by variable values',
      'Directory-per-env: divergent resources or separate access controls',
      'Workspace risk: all envs share same state bucket — IAM misconfiguration exposes prod',
      'Workspace risk: wrong workspace selection before apply is a real incident pattern',
    ],
  },
  {
    id: 'tf-004',
    category: 'terraform',
    question: 'You publish a shared Terraform module to a private registry. A consumer pins `version = "~> 2.1"`. You release v2.3.0 with a new required variable. What breaks, and how should you have handled it?',
    answer: 'The consumer\'s next `terraform init -upgrade` will pull v2.3.0, and their plan will fail because the new required variable has no default and is not set in their module call. Adding a required variable is a breaking change and should have been released as v3.0.0 per semver. Alternatively, you could add the variable with a sensible default value to keep it backward-compatible within the 2.x line.',
    hint: 'Consider what semver means for Terraform module interfaces.',
    difficulty: 'normal',
    tags: ['modules', 'versioning', 'semver'],
    keyPoints: [
      'Adding a required variable is a breaking change',
      'Should be released as v3.0.0 per semver',
      'Alternative: add variable with a default to stay backward-compatible in 2.x',
    ],
  },
  {
    id: 'tf-005',
    category: 'terraform',
    question: 'You need to bring an existing AWS resource under Terraform management. Compare `terraform import` with the `import` block introduced in Terraform 1.5. When would you prefer each?',
    answer: '`terraform import` is a CLI command that modifies state directly — you must manually write the resource block first, then run the import, then iteratively fix the config until plan shows no diff. The `import` block (Terraform 1.5+) is declarative: you add an `import { to = aws_instance.foo; id = "i-abc123" }` block in HCL, and `terraform plan` shows you what the generated config would look like. Prefer import blocks for bulk imports and CI/CD pipelines because they are reviewable in PRs and repeatable. Use CLI import for one-off resources or when you are on a Terraform version below 1.5.',
    hint: 'One approach is imperative and modifies state directly; the other is declarative and plan-visible.',
    difficulty: 'hard',
    tags: ['import', 'state', 'migration'],
    keyPoints: [
      'CLI import: imperative, modifies state directly, write config first',
      'Import block (1.5+): declarative, visible in terraform plan, reviewable in PRs',
      'Prefer import block for bulk imports and CI/CD pipelines',
      'CLI import: one-off or pre-1.5',
    ],
  },
  {
    id: 'tf-006',
    category: 'terraform',
    question: 'How does Terraform detect drift, and what are its limitations? How would you build a drift detection pipeline?',
    answer: 'Terraform detects drift by running `terraform plan`, which compares the state file against the real infrastructure via provider API calls. Its limitation is that it only checks resources it manages — if someone adds a new resource outside Terraform, it will not appear. A drift detection pipeline typically runs `terraform plan -detailed-exitcode` on a schedule (e.g., nightly cron in CI). Exit code 2 means drift detected, which triggers an alert. You should also consider that some resources have attributes that change frequently (like ECS task definition revisions) and may produce noisy false positives.',
    hint: 'Think about what `terraform plan -detailed-exitcode` returns and how you would automate it.',
    difficulty: 'hard',
    tags: ['drift', 'ci-cd', 'automation'],
    keyPoints: [
      'terraform plan compares state to real infra via provider API',
      'Limitation: only checks managed resources — new resources added outside TF are invisible',
      'terraform plan -detailed-exitcode: exit 2 = drift detected',
      'Run on a schedule (nightly cron) and alert on exit code 2',
    ],
  },
  {
    id: 'tf-007',
    category: 'terraform',
    question: 'Explain the difference between implicit and explicit dependencies in Terraform. When is `depends_on` actually necessary?',
    answer: 'Implicit dependencies are created automatically when one resource references another\'s attribute (e.g., `subnet_id = aws_subnet.main.id`). Terraform builds a DAG from these references and orders operations correctly. `depends_on` is needed when the dependency exists but is not expressed through attribute references — for example, an application server that needs an IAM policy attachment to exist before it boots, but does not reference the policy ARN in its config. Overusing `depends_on` is an anti-pattern because it forces serial execution and hides the actual relationship.',
    hint: 'When does Terraform know about a dependency automatically vs. when do you have to tell it?',
    difficulty: 'easy',
    tags: ['dependencies', 'dag', 'depends_on'],
    keyPoints: [
      'Implicit: created by attribute references, Terraform builds a DAG automatically',
      'depends_on: needed when dependency is not expressed via attribute reference',
      'Example: IAM policy attachment must exist before app boots but ARN not referenced',
      'Overusing depends_on forces serial execution — anti-pattern',
    ],
  },
  {
    id: 'tf-008',
    category: 'terraform',
    question: 'You are creating multiple IAM users. When should you use `count` vs. `for_each`, and what is the practical problem with `count` for this use case?',
    answer: 'With `count`, resources are identified by numeric index (aws_iam_user.users[0], [1], etc.). If you remove a user from the middle of the list, every subsequent resource shifts index and Terraform wants to destroy and recreate them. `for_each` uses map keys or set values as identifiers (aws_iam_user.users["alice"]), so removing one user only affects that specific resource. Use `for_each` whenever the items have a meaningful identity. `count` is fine for creating N identical resources where order does not matter (like N identical EC2 instances).',
    hint: 'Consider what happens to resource addresses when you remove the second item from a list of five.',
    difficulty: 'normal',
    tags: ['count', 'for_each', 'iteration'],
    keyPoints: [
      'count: numeric index — removing middle item shifts all subsequent indexes',
      'for_each: uses map/set keys as stable identifiers',
      'for_each: removing one item only affects that resource, not others',
      'Use for_each whenever items have a meaningful identity',
    ],
  },
  {
    id: 'tf-009',
    category: 'terraform',
    question: 'What is the difference between `locals` and `variables` in Terraform? Give an example where using a local is more appropriate.',
    answer: 'Variables are input parameters — they are set by the caller (via tfvars, CLI flags, or module arguments) and define the module\'s interface. Locals are computed values internal to the module that cannot be set externally. Use locals for derived values like `local.env_prefix = "${var.project}-${var.environment}"` that you reference in multiple resources. This avoids repeating the expression and makes it clear that the value is not a user-configurable knob. A common anti-pattern is creating a variable for something that should never be overridden by the caller.',
    hint: 'Think about who sets the value — the module consumer or the module author.',
    difficulty: 'easy',
    tags: ['locals', 'variables', 'modules'],
    keyPoints: [
      'Variables: input parameters set by caller, define module interface',
      'Locals: computed values internal to module, cannot be set externally',
      'Use locals for derived/concatenated values referenced in multiple places',
      'Anti-pattern: variable for something that should never be overridden',
    ],
  },
  {
    id: 'tf-010',
    category: 'terraform',
    question: 'Your Terraform apply fails because an AWS provider upgrade from 4.x to 5.x changed a resource schema. How should you pin provider versions to prevent this, and what is the role of the `.terraform.lock.hcl` file?',
    answer: 'Pin provider versions in the `required_providers` block using a constraint like `version = "~> 5.0"` to allow patch updates but not major version jumps. The `.terraform.lock.hcl` file records the exact provider version and its hashes after `terraform init`, ensuring all team members and CI use the same binary. It should be committed to version control. To upgrade, run `terraform init -upgrade` which updates the lock file. Without pinning, `terraform init` can pull the latest provider version, which may include breaking changes.',
    hint: 'There are two layers: the version constraint in HCL and the lock file that records exact versions.',
    difficulty: 'easy',
    tags: ['providers', 'versioning', 'lockfile'],
    keyPoints: [
      'Pin with ~> in required_providers block',
      '.terraform.lock.hcl records exact version and hashes after terraform init',
      'Commit lock file to version control',
      'terraform init -upgrade to update the lock file',
    ],
  },

  // =============================================
  // KUBERNETES (15 questions)
  // =============================================
  {
    id: 'k8s-001',
    category: 'kubernetes',
    question: 'A pod has `requests.memory: 256Mi` and `limits.memory: 512Mi`. What QoS class is it assigned, and what happens if the node is under memory pressure?',
    answer: 'This pod gets the Burstable QoS class because requests and limits are set but not equal. Under memory pressure, the kubelet evicts pods in this order: BestEffort first (no requests/limits), then Burstable (sorted by how much they exceed their request), then Guaranteed (requests == limits) last. If this pod is using 400Mi (above its 256Mi request), it is a prime eviction candidate among Burstable pods. If it exceeds 512Mi, the kernel OOM killer terminates the container immediately regardless of node pressure.',
    hint: 'QoS class depends on the relationship between requests and limits.',
    difficulty: 'normal',
    tags: ['qos', 'resources', 'eviction'],
    keyPoints: [
      'Burstable QoS: requests and limits set but not equal',
      'Eviction order: BestEffort → Burstable → Guaranteed (last)',
      'Exceeding request → prime eviction candidate among Burstable',
      'Exceeding limit → kernel OOM kills immediately',
    ],
  },
  {
    id: 'k8s-002',
    category: 'kubernetes',
    question: 'You need to autoscale a workload based on an SQS queue depth rather than CPU. Why is HPA insufficient, and how does KEDA solve this?',
    answer: 'HPA natively supports CPU and memory metrics, and can use custom metrics via the metrics API, but you need to deploy and configure a metrics adapter (like Prometheus Adapter) to expose SQS queue depth. KEDA simplifies this by providing a ScaledObject CRD with built-in scalers for AWS SQS and 50+ other event sources. KEDA also scales to zero replicas, which HPA cannot do (HPA minimum is 1). KEDA installs its own metrics adapter and feeds the metrics to HPA under the hood, so it extends rather than replaces HPA.',
    hint: 'Consider what metric sources HPA supports natively vs. what KEDA brings.',
    difficulty: 'normal',
    tags: ['hpa', 'keda', 'autoscaling'],
    keyPoints: [
      'HPA natively supports CPU and memory only',
      'Custom metrics need a metrics adapter (e.g., Prometheus Adapter)',
      'KEDA: built-in scalers for SQS and 50+ sources, no adapter needed',
      'KEDA scales to zero — HPA minimum is 1',
    ],
  },
  {
    id: 'k8s-003',
    category: 'kubernetes',
    question: 'Design a least-privilege RBAC setup for a CI/CD service account that should only be able to deploy to the "staging" namespace. What resources do you create?',
    answer: 'Create a ServiceAccount in the staging namespace, a Role (not ClusterRole) scoped to that namespace with verbs like get, list, watch, create, update, patch on resources like deployments, services, configmaps, and secrets. Bind the Role to the ServiceAccount with a RoleBinding. Using a Role instead of ClusterRole ensures the permissions cannot leak to other namespaces. Avoid wildcards on verbs or resources — enumerate exactly what the CI/CD pipeline needs. If the pipeline also needs to read cluster-scoped resources (like nodes), add a separate ClusterRoleBinding with read-only permissions.',
    hint: 'Role vs ClusterRole is the key namespace isolation decision.',
    difficulty: 'normal',
    tags: ['rbac', 'security', 'service-account'],
    keyPoints: [
      'ServiceAccount in the target namespace',
      'Role (not ClusterRole) — namespace-scoped to prevent permission leak',
      'RoleBinding to bind Role to ServiceAccount',
      'No wildcards — enumerate exact verbs and resources needed',
    ],
  },
  {
    id: 'k8s-004',
    category: 'kubernetes',
    question: 'You are rolling out a critical deployment and want to ensure at least 80% of pods remain available during voluntary disruptions. How do you configure this?',
    answer: 'Create a PodDisruptionBudget (PDB) with `minAvailable: 80%` (or equivalently `maxUnavailable: 20%`) and a selector matching the deployment\'s pods. During voluntary disruptions like node drains, `kubectl drain`, or cluster autoscaler scale-down, the eviction API respects the PDB and will block eviction if it would violate the budget. PDBs do not protect against involuntary disruptions like node crashes or OOM kills. If you set minAvailable too high (e.g., 100% with only 2 replicas), node drains will hang indefinitely.',
    hint: 'This is a specific Kubernetes resource designed for voluntary disruption protection.',
    difficulty: 'normal',
    tags: ['pdb', 'availability', 'disruptions'],
    keyPoints: [
      'PodDisruptionBudget (PDB) with minAvailable: 80% or maxUnavailable: 20%',
      'Eviction API respects PDB during voluntary disruptions (node drain, CA scale-down)',
      'Does NOT protect against involuntary disruptions (node crash, OOM)',
      '100% minAvailable with 2 replicas → node drain hangs indefinitely',
    ],
  },
  {
    id: 'k8s-005',
    category: 'kubernetes',
    question: 'Explain when you would use a liveness probe vs. a readiness probe vs. a startup probe. What happens if you misconfigure a liveness probe with a low timeout on a slow-starting app?',
    answer: 'Readiness probes gate traffic — a failing readiness probe removes the pod from Service endpoints but does not restart it. Liveness probes detect deadlocks — a failing liveness probe causes kubelet to restart the container. Startup probes run only during initial boot and disable liveness/readiness checks until they succeed, designed for slow-starting applications. If you set a liveness probe with a 5-second timeout on an app that takes 60 seconds to boot, the container enters a CrashLoopBackOff cycle because kubelet keeps killing it before it finishes starting. This is the exact use case startup probes were created to solve.',
    hint: 'Each probe type has a different consequence for failure — traffic removal, restart, or boot protection.',
    difficulty: 'normal',
    tags: ['probes', 'health-checks', 'troubleshooting'],
    keyPoints: [
      'Readiness failure: removes pod from Service endpoints, no restart',
      'Liveness failure: kubelet restarts the container',
      'Startup probe: disables liveness/readiness until app boots',
      'Low liveness timeout on slow app → CrashLoopBackOff',
    ],
  },
  {
    id: 'k8s-006',
    category: 'kubernetes',
    question: 'What is the difference between nodeSelector, node affinity, and tolerations? Describe a scenario where you need all three.',
    answer: 'nodeSelector is a simple key-value match that constrains pods to nodes with specific labels. Node affinity is a more expressive version with operators (In, NotIn, Exists) and soft/hard preferences (requiredDuringScheduling vs preferredDuringScheduling). Tolerations allow pods to be scheduled on tainted nodes but do not attract them there. Scenario: GPU workloads where GPU nodes are tainted (`nvidia.com/gpu=true:NoSchedule`), labeled (`gpu-type: a100`), and you want pods to prefer nodes in a specific AZ. You need a toleration (to allow scheduling on tainted GPU nodes), a nodeSelector or affinity (to target A100 specifically), and a preferred affinity for the AZ.',
    hint: 'One constrains which nodes, one adds expressive preferences, one permits tainted nodes.',
    difficulty: 'hard',
    tags: ['scheduling', 'affinity', 'taints', 'tolerations'],
    keyPoints: [
      'nodeSelector: simple key-value constraint',
      'Node affinity: expressive operators, soft (preferred) and hard (required) variants',
      'Tolerations: allow scheduling on tainted nodes',
      'Taints + tolerations control admission; affinity/nodeSelector control attraction',
    ],
  },
  {
    id: 'k8s-007',
    category: 'kubernetes',
    question: 'When would you use a ConfigMap vs. a Secret in Kubernetes? Are Secrets actually secure by default?',
    answer: 'ConfigMaps are for non-sensitive configuration (feature flags, config files, environment variables). Secrets are for sensitive data (passwords, tokens, TLS certs). However, Secrets are only base64-encoded by default, not encrypted — anyone with RBAC read access to Secrets in a namespace can decode them. To make Secrets actually secure, enable encryption at rest (EncryptionConfiguration with a KMS provider), restrict RBAC access to secrets, and consider using an external secrets operator (like ESO) that fetches secrets from Vault or AWS Secrets Manager at runtime rather than storing them in etcd.',
    hint: 'Base64 encoding is not encryption.',
    difficulty: 'easy',
    tags: ['configmap', 'secrets', 'security'],
    keyPoints: [
      'Secrets are base64-encoded, NOT encrypted by default',
      'Anyone with RBAC read access can decode them trivially',
      'Fix: EncryptionConfiguration with KMS for encryption at rest',
      'Better: use ESO to pull secrets from Vault/Secrets Manager at runtime',
    ],
  },
  {
    id: 'k8s-008',
    category: 'kubernetes',
    question: 'Compare Deployment, StatefulSet, and DaemonSet. You need to run a logging agent on every node — which do you use and why?',
    answer: 'Deployments manage stateless replicas with interchangeable pods. StatefulSets provide stable network identities (pod-0, pod-1), ordered deployment/scaling, and persistent volume claims per pod — used for databases and stateful workloads. DaemonSets ensure exactly one pod runs on every (or selected) node. For a logging agent, use a DaemonSet because you need one agent per node to collect that node\'s logs. DaemonSets automatically add pods when new nodes join the cluster and remove them when nodes are drained, which is exactly the behavior you want for node-level agents.',
    hint: 'Think about which controller guarantees one pod per node.',
    difficulty: 'easy',
    tags: ['deployment', 'statefulset', 'daemonset', 'workloads'],
    keyPoints: [
      'Deployment: stateless, interchangeable pods',
      'StatefulSet: stable network identity, ordered scaling, PVC per pod',
      'DaemonSet: exactly one pod per node',
      'Logging agent → DaemonSet (auto-adds/removes on node join/drain)',
    ],
  },
  {
    id: 'k8s-009',
    category: 'kubernetes',
    question: 'You want to implement a default-deny network policy in a namespace so that pods can only communicate with explicitly allowed targets. Write the policy logic.',
    answer: 'Create a NetworkPolicy with `podSelector: {}` (matches all pods in the namespace) and empty ingress and egress rules (`policyTypes: ["Ingress", "Egress"]` with no ingress/egress blocks). This denies all traffic to and from every pod in the namespace by default. Then create additional NetworkPolicies that allow specific traffic — for example, allowing ingress on port 8080 from pods with label `app: frontend`. NetworkPolicies are additive, so each new policy opens up specific paths without revoking the default-deny. Note: this requires a CNI that supports NetworkPolicy (Calico, Cilium) — the default kubenet does not enforce them.',
    hint: 'An empty podSelector matches everything; empty rules deny everything.',
    difficulty: 'hard',
    tags: ['network-policy', 'security', 'zero-trust'],
    keyPoints: [
      'podSelector: {} matches all pods in namespace',
      'policyTypes: Ingress + Egress with no rules = deny all',
      'NetworkPolicies are additive — additional policies open specific paths',
      'Requires CNI that enforces NetworkPolicy (Calico, Cilium — not default kubenet)',
    ],
  },
  {
    id: 'k8s-010',
    category: 'kubernetes',
    question: 'A pod is being evicted from a node. Explain the eviction priority order and how resource requests affect which pods get evicted first.',
    answer: 'When a node hits a resource threshold (memory.available < 100Mi by default), the kubelet evicts pods based on QoS class: BestEffort pods (no requests/limits) are evicted first, then Burstable pods sorted by how much they exceed their requests relative to the node\'s capacity, then Guaranteed pods last. Within the same QoS class, pods using more resources relative to their requests are evicted first. This is why setting accurate requests is critical — a pod requesting 100Mi but using 900Mi is a high-priority eviction target. Priority classes (PriorityClass) also factor in, with lower-priority pods evicted before higher-priority ones.',
    hint: 'QoS class is the primary sort key, then actual usage vs. request ratio.',
    difficulty: 'hard',
    tags: ['eviction', 'qos', 'scheduling'],
    keyPoints: [
      'Eviction order: BestEffort → Burstable → Guaranteed',
      'Within Burstable: sorted by excess usage over request',
      'Accurate requests are critical — under-requesting makes you a prime eviction target',
      'PriorityClass also factors in within same QoS class',
    ],
  },
  {
    id: 'k8s-011',
    category: 'kubernetes',
    question: 'A pod is in CrashLoopBackOff. Walk through your debugging steps.',
    answer: 'First, `kubectl describe pod <name>` to check the Events section for OOMKilled, image pull errors, or failed mounts. Then `kubectl logs <pod> --previous` to see the last container\'s stdout/stderr before it crashed. Check if it is an application error (stack trace in logs), a misconfigured command/args, a missing environment variable or config, or a failed health check killing the container. If logs are empty, the process may be crashing before writing output — try running the image locally with `docker run`. The exponential backoff (10s, 20s, 40s up to 5min) means you need to wait between restarts, or delete the pod to reset the backoff timer.',
    hint: 'Start with describe, then previous logs, then check events for the root cause.',
    difficulty: 'easy',
    tags: ['troubleshooting', 'crashloop', 'debugging'],
    keyPoints: [
      'kubectl describe pod: check Events for OOMKilled, image pull, mount errors',
      'kubectl logs --previous: last container output before crash',
      'Empty logs = process crashes before writing output',
      'Delete pod to reset exponential backoff timer',
    ],
  },
  {
    id: 'k8s-012',
    category: 'kubernetes',
    question: 'What is the difference between a container being OOMKilled and being CPU throttled? How do you identify each?',
    answer: 'OOMKilled means the container exceeded its memory limit and the kernel killed the process (exit code 137). You see it in `kubectl describe pod` as `reason: OOMKilled`. CPU throttling means the container is using more CPU than its limit and the kernel CFS scheduler forces it to wait. The container stays running but becomes slow. You detect throttling via `container_cpu_cfs_throttled_periods_total` Prometheus metric. OOMKilled requires raising the memory limit or fixing a memory leak. Throttling requires raising the CPU limit or, increasingly, the recommendation is to set CPU requests but not CPU limits to avoid unnecessary throttling.',
    hint: 'One kills the process; the other slows it down.',
    difficulty: 'normal',
    tags: ['oomkilled', 'throttling', 'resources', 'troubleshooting'],
    keyPoints: [
      'OOMKilled: exceeded memory limit, process killed (exit code 137)',
      'CPU throttling: container slowed by CFS scheduler, stays running',
      'Detect throttling: container_cpu_cfs_throttled_periods_total metric',
      'Recommendation: set CPU requests but not CPU limits to avoid throttling',
    ],
  },
  {
    id: 'k8s-013',
    category: 'kubernetes',
    question: 'How does namespace isolation work in Kubernetes? What does a namespace actually isolate vs. what it does not?',
    answer: 'Namespaces provide a scope for names (two deployments can be called "api" in different namespaces), RBAC boundaries (Roles are namespace-scoped), resource quotas, and network policy targets. However, namespaces do NOT isolate network traffic by default (pods can talk across namespaces freely without NetworkPolicies), do not isolate node resources (pods from different namespaces share the same node), and do not isolate cluster-scoped resources (ClusterRoles, Nodes, PersistentVolumes, Namespaces themselves). True multi-tenancy requires NetworkPolicies, ResourceQuotas, and potentially separate clusters.',
    hint: 'Namespaces are a soft boundary, not a security boundary, unless you add policies.',
    difficulty: 'normal',
    tags: ['namespaces', 'isolation', 'multi-tenancy'],
    keyPoints: [
      'Namespaces DO isolate: names, RBAC roles, resource quotas',
      'Namespaces do NOT isolate: network traffic (pods can talk cross-namespace)',
      'Namespaces do NOT isolate: node resources or cluster-scoped resources',
      'True multi-tenancy needs NetworkPolicies + ResourceQuotas (or separate clusters)',
    ],
  },
  {
    id: 'k8s-014',
    category: 'kubernetes',
    question: 'Explain how IRSA (IAM Roles for Service Accounts) works in EKS. Why is it preferred over attaching an IAM role to the EC2 node?',
    answer: 'IRSA uses an OIDC identity provider registered with IAM. When a pod uses a Kubernetes ServiceAccount annotated with `eks.amazonaws.com/role-arn`, the EKS pod identity webhook injects a projected service account token and AWS environment variables. The AWS SDK exchanges this OIDC token for temporary IAM credentials scoped to that specific role. This is preferred over node IAM roles because node roles grant the same permissions to every pod on that node, violating least privilege. With IRSA, each pod gets only the IAM permissions its workload needs, and the trust policy restricts which namespace/service-account can assume the role.',
    hint: 'Think about the granularity: per-node vs. per-pod IAM permissions.',
    difficulty: 'hard',
    tags: ['irsa', 'iam', 'eks', 'security'],
    keyPoints: [
      'IRSA uses OIDC provider registered with IAM',
      'ServiceAccount annotated with role ARN; webhook injects token + env vars',
      'SDK exchanges OIDC token for temporary scoped IAM credentials',
      'Node role: all pods on node share same permissions — violates least privilege',
    ],
  },
  {
    id: 'k8s-015',
    category: 'kubernetes',
    question: 'Compare the RollingUpdate and Recreate deployment strategies. When would you deliberately choose Recreate over RollingUpdate?',
    answer: 'RollingUpdate gradually replaces old pods with new ones, controlled by `maxSurge` and `maxUnavailable`, ensuring zero-downtime during deploys. Recreate kills all old pods before creating new ones, causing downtime. Choose Recreate when your application cannot tolerate two versions running simultaneously — for example, a database migration runner, a workload with exclusive access to a volume (RWO PersistentVolume), or a legacy app where running two versions causes data corruption. RollingUpdate is the default and preferred for stateless web services.',
    hint: 'When would running old and new versions simultaneously cause problems?',
    difficulty: 'easy',
    tags: ['deployment', 'strategy', 'rolling-update'],
    keyPoints: [
      'RollingUpdate: gradual replacement, zero-downtime, maxSurge/maxUnavailable',
      'Recreate: kills all old pods first — causes downtime',
      'Use Recreate when two versions cannot run simultaneously',
      'Examples: DB migration runner, exclusive RWO volume, data-corrupting legacy app',
    ],
  },

  // =============================================
  // EKS & AWS (10 questions)
  // =============================================
  {
    id: 'eks-001',
    category: 'eks-aws',
    question: 'Compare EKS managed node groups, self-managed node groups, and Fargate profiles. When would you choose each?',
    answer: 'Managed node groups handle AMI updates, node provisioning, and graceful draining during upgrades — best for most workloads where you want AWS to manage the lifecycle. Self-managed node groups (via ASGs) give full control over the AMI, bootstrap script, and instance configuration — needed for custom AMIs, GPU nodes with specific drivers, or Windows nodes. Fargate profiles run pods without managing nodes at all — good for burst workloads, batch jobs, or when you want zero node management, but they have limitations (no DaemonSets, no EBS volumes, higher per-pod cost, slower pod startup).',
    hint: 'Think about the tradeoff between operational control and management overhead.',
    difficulty: 'normal',
    tags: ['node-groups', 'fargate', 'eks'],
    keyPoints: [
      'Managed node groups: AWS handles AMI, provisioning, draining',
      'Self-managed: full AMI control, needed for custom/GPU/Windows nodes',
      'Fargate: no node management, but no DaemonSets, no EBS',
      'Fargate: higher per-pod cost and slower pod startup',
    ],
  },
  {
    id: 'eks-002',
    category: 'eks-aws',
    question: 'Why would you choose Karpenter over Cluster Autoscaler for EKS, and what is the key architectural difference?',
    answer: 'Cluster Autoscaler works with ASGs — it adjusts the desired count of pre-defined node groups, meaning you need to create separate ASGs for different instance types. Karpenter provisions nodes directly via EC2 Fleet API, bypassing ASGs entirely. It evaluates pending pod requirements and selects optimal instance types, sizes, and availability zones in real-time. Karpenter is faster (30-60 seconds vs. minutes for CA), handles heterogeneous workloads better (no need for many ASGs), supports consolidation (replacing underutilized nodes with smaller ones), and can use spot instances more effectively with diversified allocation strategies.',
    hint: 'One works through ASGs; the other provisions EC2 instances directly.',
    difficulty: 'normal',
    tags: ['karpenter', 'cluster-autoscaler', 'scaling'],
    keyPoints: [
      'CA: works through ASGs, adjusts desired count of pre-defined node groups',
      'Karpenter: provisions EC2 directly via EC2 Fleet API, no ASGs needed',
      'Karpenter: faster (30-60s vs minutes), consolidation support',
      'Karpenter: better spot diversification across instance types',
    ],
  },
  {
    id: 'eks-003',
    category: 'eks-aws',
    question: 'Explain the difference between the aws-auth ConfigMap and EKS Access Entries for managing cluster access. Why is AWS moving toward Access Entries?',
    answer: 'The aws-auth ConfigMap maps IAM principals (roles/users) to Kubernetes RBAC groups, and it lives inside the cluster as a regular ConfigMap in kube-system. If it gets corrupted or deleted, you can lock everyone out of the cluster. EKS Access Entries (GA since 2024) manage the same mappings via the EKS API, outside the cluster. They are manageable via Terraform/CloudFormation, support access policies for common patterns, and cannot be accidentally deleted by a kubectl command. AWS is moving toward Access Entries because they are more robust, auditable via CloudTrail, and avoid the single-point-of-failure risk of a ConfigMap.',
    hint: 'One is a Kubernetes resource inside the cluster; the other is an AWS API resource outside it.',
    difficulty: 'hard',
    tags: ['aws-auth', 'access-entries', 'iam', 'security'],
    keyPoints: [
      'aws-auth: ConfigMap in kube-system, inside cluster — delete it and lose all access',
      'Access Entries: managed via EKS API, outside the cluster',
      'Access Entries: manageable via Terraform, auditable via CloudTrail',
      'Access Entries preferred: no single-point-of-failure ConfigMap risk',
    ],
  },
  {
    id: 'eks-004',
    category: 'eks-aws',
    question: 'You are upgrading an EKS add-on (CoreDNS) and the upgrade fails, leaving DNS resolution broken. What is the gotcha with EKS managed add-ons?',
    answer: 'EKS managed add-ons can overwrite custom configurations you made to the add-on. If you customized the CoreDNS ConfigMap (e.g., added custom forward rules) or modified the deployment (resource limits, replicas), an add-on update may revert those changes. The fix is to use the `configurationValues` field in the add-on configuration to pass your customizations through the managed add-on API. Additionally, add-on version compatibility with the EKS cluster version can cause failures — always check the compatibility matrix before upgrading. The `PRESERVE` conflict resolution strategy can help retain custom settings.',
    hint: 'Think about what happens to manual changes when AWS reconciles the add-on.',
    difficulty: 'hard',
    tags: ['add-ons', 'coredns', 'upgrades'],
    keyPoints: [
      'Managed add-ons can overwrite manually customized configurations',
      'Use configurationValues field to pass customizations through the add-on API',
      'Check compatibility matrix before upgrading add-ons',
      'PRESERVE conflict resolution strategy retains custom settings',
    ],
  },
  {
    id: 'eks-005',
    category: 'eks-aws',
    question: 'Your EKS pods are failing to get IP addresses and are stuck in ContainerCreating. What is likely happening with the VPC CNI, and how do you prevent IP exhaustion?',
    answer: 'The AWS VPC CNI assigns real VPC IP addresses to each pod from the subnet CIDR. When the subnet runs out of IPs, new pods cannot be scheduled. This is common in /24 subnets (254 usable IPs) with many small pods. Solutions: (1) use larger subnets (/19 or /18), (2) enable prefix delegation (`ENABLE_PREFIX_DELEGATION=true`) which assigns /28 prefixes to nodes instead of individual IPs, giving 16x more IPs per ENI slot, (3) use secondary CIDR ranges (100.64.0.0/16) for pod networking, or (4) configure `WARM_IP_TARGET` and `MINIMUM_IP_TARGET` to control how aggressively the CNI pre-allocates IPs.',
    hint: 'Each pod gets a real VPC IP — what happens when the subnet CIDR is too small?',
    difficulty: 'expert',
    tags: ['vpc-cni', 'networking', 'ip-exhaustion'],
    keyPoints: [
      'VPC CNI assigns real VPC IPs to pods — subnet exhaustion = pods stuck in ContainerCreating',
      'Fix 1: use larger subnets (/19 or /18)',
      'Fix 2: enable prefix delegation (ENABLE_PREFIX_DELEGATION=true) — 16x more IPs per ENI',
      'Fix 3: secondary CIDR ranges (100.64.0.0/16) for pod networking',
    ],
  },
  {
    id: 'eks-006',
    category: 'eks-aws',
    question: 'Compare IRSA (IAM Roles for Service Accounts) with attaching an IAM instance profile to the node. What is the security risk of using node-level IAM?',
    answer: 'With node IAM roles, every pod on that node can access the same IAM permissions by hitting the instance metadata service (169.254.169.254). A compromised pod can escalate privileges by assuming the node role, which typically has broad permissions (ECR pull, CloudWatch, etc.). IRSA scopes credentials to individual Kubernetes ServiceAccounts, so a pod in namespace A cannot access credentials meant for namespace B. Additionally, IRSA credentials are delivered via projected service account tokens with audience scoping, not via the instance metadata endpoint. To fully mitigate node role risk, also enforce IMDSv2 with hop limit 1 to prevent pods from reaching the metadata service.',
    hint: 'Consider what happens when any pod on a node can reach the metadata service.',
    difficulty: 'normal',
    tags: ['irsa', 'iam', 'security', 'metadata'],
    keyPoints: [
      'Node IAM: all pods can hit instance metadata (169.254.169.254) and get node credentials',
      'Compromised pod can escalate via node role (ECR, CloudWatch, etc.)',
      'IRSA: credentials scoped to individual ServiceAccount, not accessible across namespaces',
      'Mitigate further: enforce IMDSv2 with hop limit 1 to block pods from metadata service',
    ],
  },
  {
    id: 'eks-007',
    category: 'eks-aws',
    question: 'What EKS control plane logs should you enable, and how do you use them for troubleshooting?',
    answer: 'EKS supports five log types sent to CloudWatch Logs: api (API server requests), audit (who did what), authenticator (IAM-to-K8s auth), controllerManager, and scheduler. For security, always enable audit logs — they record every API call with user identity, which is critical for incident investigation. Enable authenticator logs to debug RBAC/IAM issues (why a user gets 403). API server logs help debug webhook failures or API latency. Controller and scheduler logs are useful for debugging pod scheduling failures. These logs can be expensive at scale, so consider setting retention policies and using CloudWatch Insights for querying.',
    hint: 'Five log types — which ones are essential for security vs. debugging?',
    difficulty: 'normal',
    tags: ['logging', 'cloudwatch', 'security', 'troubleshooting'],
    keyPoints: [
      'Five types: api, audit, authenticator, controllerManager, scheduler',
      'Always enable audit: records every API call with user identity',
      'Authenticator: debug RBAC/IAM 403 issues',
      'Set retention policies — expensive at scale',
    ],
  },
  {
    id: 'eks-008',
    category: 'eks-aws',
    question: 'When would you use an ALB Ingress Controller vs. an NLB for exposing services in EKS?',
    answer: 'ALB (via AWS Load Balancer Controller) operates at Layer 7 — it supports path-based and host-based routing, HTTP/2, WebSockets, WAF integration, and can route to multiple services from a single ALB using Ingress rules. NLB operates at Layer 4 — it handles raw TCP/UDP, provides static IPs, supports TLS passthrough, has lower latency, and preserves client source IPs. Use ALB for HTTP microservices where you need routing rules and WAF. Use NLB for gRPC (with TLS passthrough), non-HTTP protocols, when you need static IPs (e.g., for whitelisting), or when you need extreme low-latency performance.',
    hint: 'Layer 7 vs Layer 4 — what routing capabilities do you need?',
    difficulty: 'normal',
    tags: ['alb', 'nlb', 'ingress', 'networking'],
    keyPoints: [
      'ALB: Layer 7 — path/host routing, WAF, HTTP/2, single LB for many services',
      'NLB: Layer 4 — TCP/UDP, static IPs, TLS passthrough, lower latency',
      'Use ALB for HTTP microservices with routing rules',
      'Use NLB for gRPC, non-HTTP, static IPs, or ultra-low latency',
    ],
  },
  {
    id: 'eks-009',
    category: 'eks-aws',
    question: 'Walk through the EKS cluster version upgrade process. What is the correct order of operations and what can go wrong?',
    answer: 'Order: (1) Review the Kubernetes changelog and deprecation guide for the target version. (2) Upgrade the control plane via `aws eks update-cluster-version` — this is non-disruptive as AWS runs it in-place. (3) Update EKS managed add-ons (CoreDNS, kube-proxy, VPC CNI) to versions compatible with the new K8s version. (4) Upgrade node groups — for managed node groups, update the launch template AMI and the node group version; nodes are drained and replaced gracefully. What can go wrong: deprecated APIs in your manifests break (e.g., `extensions/v1beta1` removed), add-on compatibility issues, custom admission webhooks incompatible with new API versions, and PDBs blocking node drains.',
    hint: 'Control plane first, then add-ons, then nodes — never the reverse.',
    difficulty: 'hard',
    tags: ['upgrades', 'version', 'lifecycle'],
    keyPoints: [
      'Order: control plane → add-ons → node groups',
      'Control plane upgrade is non-disruptive (AWS runs in-place)',
      'Deprecated API versions in manifests will break after upgrade',
      'PDBs can block node drains during node group upgrade',
    ],
  },
  {
    id: 'eks-010',
    category: 'eks-aws',
    question: 'How would you set up cross-account access to an EKS cluster? An SRE in Account B needs kubectl access to a cluster in Account A.',
    answer: 'Create an IAM role in Account A with a trust policy allowing the SRE\'s role in Account B to assume it. Map this IAM role to a Kubernetes RBAC group in the aws-auth ConfigMap or EKS Access Entry. The SRE assumes the cross-account role via `aws sts assume-role`, then runs `aws eks update-kubeconfig --cluster-name <name> --role-arn <role-in-account-a>`. The kubeconfig will automatically assume the role when issuing kubectl commands. For Access Entries, you can directly associate the Account B role ARN with an access policy, which is simpler than the aws-auth approach.',
    hint: 'Combine IAM cross-account role assumption with Kubernetes RBAC mapping.',
    difficulty: 'hard',
    tags: ['cross-account', 'iam', 'rbac'],
    keyPoints: [
      'IAM role in Account A with trust policy allowing Account B role to assume it',
      'Map the role to a K8s RBAC group via aws-auth or Access Entry',
      'SRE assumes role with aws sts assume-role',
      'aws eks update-kubeconfig --role-arn for kubeconfig setup',
    ],
  },

  // =============================================
  // CI/CD & GITOPS (10 questions)
  // =============================================
  {
    id: 'cicd-001',
    category: 'cicd-gitops',
    question: 'Explain ArgoCD sync waves and give an example of when ordering matters in a deployment.',
    answer: 'Sync waves are annotations (`argocd.argoproj.io/sync-wave: "1"`) that control the order in which resources are applied. Lower wave numbers are applied first, and ArgoCD waits for resources in each wave to be healthy before proceeding to the next. Example: wave 0 for Namespace and RBAC resources, wave 1 for ConfigMaps and Secrets, wave 2 for database StatefulSet, wave 3 for application Deployments that depend on the database. Without sync waves, ArgoCD applies all resources simultaneously, and a Deployment might start before its ConfigMap exists, causing CrashLoopBackOff on missing environment variables.',
    hint: 'Think about resources that must exist before other resources can start successfully.',
    difficulty: 'normal',
    tags: ['argocd', 'sync-waves', 'ordering'],
    keyPoints: [
      'Annotation: argocd.argoproj.io/sync-wave — lower number applied first',
      'ArgoCD waits for each wave to be healthy before proceeding',
      'Without waves: all resources applied simultaneously — race conditions',
      'Example order: Namespace → ConfigMaps/Secrets → Database → App Deployment',
    ],
  },
  {
    id: 'cicd-002',
    category: 'cicd-gitops',
    question: 'Describe the ArgoCD App of Apps pattern. What problem does it solve, and what is a downside?',
    answer: 'App of Apps is a pattern where a root ArgoCD Application points to a Git directory containing other Application manifests. When ArgoCD syncs the root app, it creates all the child Applications, which in turn deploy their own workloads. This solves the bootstrapping problem: instead of manually creating 50 Applications, you create one root app and everything else is declarative. The downside is cascading failures — if the root app is misconfigured or accidentally deleted with `cascade=true`, all child apps and their resources are deleted. It also creates a complex dependency tree that can be hard to debug when sync fails.',
    hint: 'One Application that manages other Applications — a recursive pattern.',
    difficulty: 'normal',
    tags: ['argocd', 'app-of-apps', 'patterns'],
    keyPoints: [
      'Root Application points to Git directory with child Application manifests',
      'Solves bootstrapping: one root app creates all others declaratively',
      'Risk: cascade delete of root app deletes all children and their resources',
      'Complex dependency tree — hard to debug sync failures',
    ],
  },
  {
    id: 'cicd-003',
    category: 'cicd-gitops',
    question: 'When would you use ArgoCD ApplicationSets instead of the App of Apps pattern?',
    answer: 'ApplicationSets are a templating mechanism that generates multiple Applications from a single template and a generator (list, Git directory, cluster, matrix, etc.). Use ApplicationSets when you have a repetitive pattern — for example, deploying the same microservice to 10 clusters, or creating one Application per directory in a monorepo. App of Apps requires you to manually write each Application manifest. ApplicationSets are more DRY and scale better. The Git generator is particularly powerful: it discovers directories in a repo and creates an Application per directory automatically. ApplicationSets are the recommended approach for multi-cluster and multi-tenant scenarios.',
    hint: 'One requires writing each Application manifest; the other generates them from a template.',
    difficulty: 'hard',
    tags: ['argocd', 'applicationsets', 'multi-cluster'],
    keyPoints: [
      'ApplicationSets generate multiple Applications from a template + generator',
      'App of Apps: write each Application manifest manually',
      'ApplicationSets: DRY, scale to many clusters without manual manifests',
      'Git generator: auto-discovers directories, creates one App per directory',
    ],
  },
  {
    id: 'cicd-004',
    category: 'cicd-gitops',
    question: 'Compare External Secrets Operator (ESO) and Sealed Secrets for managing secrets in a GitOps workflow. When would you pick each?',
    answer: 'Sealed Secrets encrypts secrets client-side with `kubeseal` using the cluster\'s public key, and the encrypted SealedSecret CRD can be safely committed to Git. The controller in-cluster decrypts them into regular Secrets. ESO syncs secrets from external stores (AWS Secrets Manager, Vault, GCP SM) into Kubernetes Secrets via an ExternalSecret CRD. Pick ESO when you already have a secrets manager and want a single source of truth for secrets across K8s and non-K8s consumers. Pick Sealed Secrets for simpler setups where you want everything in Git and do not want to manage an external secrets store. ESO advantage: secret rotation happens externally and ESO polls for updates.',
    hint: 'One encrypts secrets into Git; the other references secrets from an external store.',
    difficulty: 'normal',
    tags: ['secrets', 'eso', 'sealed-secrets', 'gitops'],
    keyPoints: [
      'Sealed Secrets: encrypted by kubeseal, stored in Git, decrypted in-cluster',
      'ESO: syncs from external store (Secrets Manager, Vault) into K8s Secrets',
      'ESO: single source of truth, rotation happens externally',
      'Sealed Secrets: simpler, everything in Git, no external store needed',
    ],
  },
  {
    id: 'cicd-005',
    category: 'cicd-gitops',
    question: 'What does "single source of truth" mean in GitOps, and what happens when someone runs `kubectl edit` to modify a Deployment that ArgoCD manages?',
    answer: 'In GitOps, the Git repository is the single source of truth — the desired state of all infrastructure and applications is declared in Git. When someone runs `kubectl edit` on an ArgoCD-managed resource, ArgoCD detects the drift on its next sync check (default every 3 minutes). If auto-sync is enabled, ArgoCD reverts the change to match Git. If auto-sync is disabled, ArgoCD shows the Application as "OutOfSync." This is why `self-heal: true` is important for production — it ensures manual changes are always reverted. Without it, the cluster can silently drift from the declared state.',
    hint: 'What happens when the cluster state diverges from what Git declares?',
    difficulty: 'easy',
    tags: ['gitops', 'drift', 'argocd', 'self-heal'],
    keyPoints: [
      'Git is single source of truth — cluster state must match Git',
      'kubectl edit drift detected on next sync cycle (default 3 minutes)',
      'Auto-sync enabled: ArgoCD reverts manual changes',
      'self-heal: true ensures drift is always corrected automatically',
    ],
  },
  {
    id: 'cicd-006',
    category: 'cicd-gitops',
    question: 'How do you securely manage secrets in GitHub Actions? What are the scoping options, and what is a common mistake?',
    answer: 'GitHub Actions secrets are stored encrypted and exposed as environment variables in workflows. They can be scoped to organization, repository, or environment level. Environment secrets require deployment approval rules before the job runs — this is useful for protecting prod secrets behind a manual approval gate. A common mistake is echoing secrets in logs via `run: echo ${{ secrets.API_KEY }}` or using them in step names where they appear in the workflow UI. GitHub masks known secret values in logs, but derived values (base64-encoded, concatenated) may leak. Use OIDC federation (`aws-actions/configure-aws-credentials` with role-to-assume) instead of long-lived IAM keys.',
    hint: 'Consider scoping levels and what happens when a secret value appears in a log.',
    difficulty: 'normal',
    tags: ['github-actions', 'secrets', 'security'],
    keyPoints: [
      'Scoped to: organization, repository, or environment level',
      'Environment secrets need approval rules (gate for prod secrets)',
      'Never echo secrets in logs — derived values (base64, concatenated) bypass masking',
      'Use OIDC federation instead of long-lived IAM keys',
    ],
  },
  {
    id: 'cicd-007',
    category: 'cicd-gitops',
    question: 'Compare canary and blue-green deployment strategies in Argo Rollouts. When would you choose one over the other?',
    answer: 'Blue-green maintains two full environments — the active (blue) and the standby (green). You deploy to green, test it, then switch traffic instantly by updating the Service selector. Rollback is instant (switch back). Canary gradually shifts traffic (e.g., 5%, 25%, 50%, 100%) to the new version with analysis gates between steps. Choose blue-green when you need instant rollback and can afford running double infrastructure. Choose canary when you want to limit blast radius by testing with a small percentage of real traffic first. Argo Rollouts implements both patterns with analysis steps that can auto-promote or rollback based on metrics (Prometheus queries, web hooks).',
    hint: 'One shifts traffic gradually with analysis; the other swaps all traffic at once.',
    difficulty: 'normal',
    tags: ['argo-rollouts', 'canary', 'blue-green', 'deployment'],
    keyPoints: [
      'Blue-green: two full environments, instant traffic switch and rollback',
      'Canary: gradual traffic shift (5% → 25% → 100%) with analysis gates',
      'Blue-green: instant rollback, double infrastructure cost',
      'Canary: limits blast radius, requires running two versions simultaneously',
    ],
  },
  {
    id: 'cicd-008',
    category: 'cicd-gitops',
    question: 'How does ArgoCD detect drift in your cluster? What is the difference between "OutOfSync" and "Degraded" health status?',
    answer: 'ArgoCD periodically (default 3 minutes) compares the live state of Kubernetes resources against the desired state in Git by computing a diff. "OutOfSync" means the live state differs from Git — someone edited a resource, a webhook mutated it, or a new commit has not been synced yet. "Degraded" is a health status meaning the resource exists and matches Git, but is unhealthy (e.g., a Deployment with unavailable replicas, a pod in CrashLoopBackOff). You can be "Synced + Degraded" (matches Git but broken) or "OutOfSync + Healthy" (drifted but still working). Both require attention but for different reasons.',
    hint: 'Sync status is about Git match; health status is about runtime condition.',
    difficulty: 'normal',
    tags: ['argocd', 'drift', 'health', 'sync'],
    keyPoints: [
      'ArgoCD compares live state to Git every 3 minutes (default)',
      'OutOfSync: live state differs from Git',
      'Degraded: matches Git but unhealthy runtime (CrashLoopBackOff, unavailable replicas)',
      'Can be Synced + Degraded or OutOfSync + Healthy simultaneously',
    ],
  },
  {
    id: 'cicd-009',
    category: 'cicd-gitops',
    question: 'How would you set up ArgoCD to manage workloads across multiple clusters? What are the main architectural approaches?',
    answer: 'ArgoCD supports multi-cluster by registering external clusters via `argocd cluster add`. The hub-spoke model runs one ArgoCD instance on a management cluster that deploys to multiple target clusters. ApplicationSets with the cluster generator can template Applications per registered cluster. Alternatively, you can run separate ArgoCD instances per cluster (decentralized model) for isolation and reduced blast radius, managed by a meta ArgoCD on a control plane cluster. The hub-spoke model is simpler to manage but creates a single point of failure. The decentralized model is more resilient but harder to maintain consistency. Network connectivity (VPN, peering) between the management cluster and targets is a prerequisite.',
    hint: 'Hub-spoke (centralized) vs. decentralized — what are the tradeoffs?',
    difficulty: 'expert',
    tags: ['argocd', 'multi-cluster', 'architecture'],
    keyPoints: [
      'Register clusters via argocd cluster add',
      'Hub-spoke: one ArgoCD on management cluster deploying to all targets',
      'Decentralized: separate ArgoCD per cluster, meta ArgoCD manages them',
      'Hub-spoke: simpler but single point of failure; decentralized: resilient but complex',
    ],
  },
  {
    id: 'cicd-010',
    category: 'cicd-gitops',
    question: 'Compare push-based CD (CI pipeline runs kubectl apply) vs. pull-based CD (ArgoCD/Flux). What are the security implications?',
    answer: 'Push-based CD requires the CI system to have cluster credentials (kubeconfig, service account token), which creates a wide attack surface — if CI is compromised, the attacker has direct cluster access. Pull-based CD (GitOps) inverts this: the agent inside the cluster pulls changes from Git, so no external system needs cluster credentials. Git becomes the only interface, and you control access via Git permissions. Pull-based also provides automatic drift correction. The trade-off is that push-based is simpler to set up and more familiar, and provides immediate feedback in the CI pipeline. Pull-based requires running a controller in-cluster and has an eventual consistency model (sync interval delay).',
    hint: 'Think about where cluster credentials live in each model.',
    difficulty: 'normal',
    tags: ['gitops', 'ci-cd', 'security', 'push-vs-pull'],
    keyPoints: [
      'Push-based: CI holds cluster credentials — compromised CI = cluster access',
      'Pull-based: agent in cluster pulls from Git, no external system needs credentials',
      'Pull-based: automatic drift correction, Git is the only interface',
      'Push-based: simpler, immediate feedback; Pull: eventual consistency (sync delay)',
    ],
  },

  // =============================================
  // ARCHITECTURE (10 questions)
  // =============================================
  {
    id: 'arch-001',
    category: 'architecture',
    question: 'Design a multi-account AWS strategy for a company with 5 product teams. What accounts do you create and why?',
    answer: 'Use AWS Organizations with at least these accounts: a management/root account (billing, organization policies only — no workloads), a shared-services account (CI/CD, artifact registries, shared monitoring), a networking/transit account (Transit Gateway, DNS, VPN), a security/audit account (CloudTrail aggregation, GuardDuty, Security Hub), and per-team accounts for dev/staging/prod (15 accounts for 5 teams x 3 environments). Use SCPs to enforce guardrails (deny regions, prevent disabling CloudTrail). This structure provides blast radius isolation — a misconfiguration in Team A\'s dev account cannot affect Team B\'s prod. Use Terraform with Terragrunt to manage the account vending process.',
    hint: 'Think about blast radius isolation and what resources should be shared vs. separated.',
    difficulty: 'expert',
    tags: ['multi-account', 'aws-organizations', 'scp'],
    keyPoints: [
      'Management/root: billing and org policies only — no workloads',
      'Separate accounts: shared-services, networking/transit, security/audit',
      'Per-team dev/staging/prod accounts (15 for 5 teams × 3 envs)',
      'SCPs for guardrails; blast radius isolation between teams',
    ],
  },
  {
    id: 'arch-002',
    category: 'architecture',
    question: 'You need to deploy a new version of a service with zero downtime. The service has a database migration that adds a non-nullable column. How do you handle this?',
    answer: 'Use the expand-contract (parallel change) pattern. Phase 1 (expand): add the column as nullable, deploy new code that writes to the new column but does not require it. Phase 2: backfill existing rows with the default value. Phase 3 (contract): add the NOT NULL constraint, deploy code that requires the column, remove old column reads. Each phase is independently deployable and rollback-safe. Never make a breaking schema change in a single deployment — if you add a non-nullable column and deploy new code simultaneously, the old pods (still running during rolling update) will fail on INSERT because they do not supply the new column. This is one of the hardest zero-downtime patterns to get right.',
    hint: 'You cannot deploy a breaking schema change and new code at the same time in a rolling update.',
    difficulty: 'expert',
    tags: ['zero-downtime', 'database', 'migrations', 'deployment'],
    keyPoints: [
      'Expand-contract (parallel change) pattern',
      'Phase 1: add column as nullable, write to it without requiring it',
      'Phase 2: backfill existing rows',
      'Phase 3: add NOT NULL constraint, remove old column reads',
    ],
  },
  {
    id: 'arch-003',
    category: 'architecture',
    question: 'How do you rotate a database password without downtime when the password is used by 10 microservices running on EKS?',
    answer: 'Use AWS Secrets Manager with automatic rotation enabled. Store the DB password in Secrets Manager, and use External Secrets Operator (ESO) to sync it into Kubernetes Secrets. Secrets Manager rotation uses a dual-user strategy: it creates a new user with the new password (AWSCURRENT) while keeping the old one valid (AWSPREVIOUS). During rotation, both passwords work simultaneously. ESO polls Secrets Manager on a refresh interval (e.g., 1 minute) and updates the K8s Secret. Services need to either watch for Secret changes and reload, or use a sidecar/init-container that refreshes credentials. The critical gap is the time between rotation and pod credential refresh — keep the old password valid during this window.',
    hint: 'Two passwords must be valid simultaneously during the transition window.',
    difficulty: 'expert',
    tags: ['secrets', 'rotation', 'zero-downtime', 'eso'],
    keyPoints: [
      'Secrets Manager dual-user strategy: AWSCURRENT + AWSPREVIOUS both valid',
      'ESO polls Secrets Manager and updates K8s Secret on refresh interval',
      'Services must reload credentials when Secret changes (watch or sidecar)',
      'Keep old password valid until all pods have refreshed their credentials',
    ],
  },
  {
    id: 'arch-004',
    category: 'architecture',
    question: 'Design a highly available EKS cluster. What are the key decisions for multi-AZ resilience?',
    answer: 'Run the EKS control plane across 3 AZs (AWS manages this automatically). Place node groups in at least 3 AZs with subnets in each. Use PodTopologySpreadConstraints to distribute replicas across AZs evenly (`maxSkew: 1, topologyKey: topology.kubernetes.io/zone`). Configure PodDisruptionBudgets to prevent simultaneous pod eviction during node drains. Use EBS gp3 volumes with awareness that they are AZ-bound (StatefulSet pods cannot failover to another AZ without data replication). For stateful workloads, consider EFS (multi-AZ) or application-level replication. Set up cross-AZ-aware monitoring because cross-AZ data transfer has costs and latency implications.',
    hint: 'Control plane, node placement, pod distribution, and storage all need AZ awareness.',
    difficulty: 'hard',
    tags: ['high-availability', 'multi-az', 'eks', 'topology'],
    keyPoints: [
      'Node groups in 3+ AZs with subnets in each',
      'PodTopologySpreadConstraints to spread replicas across AZs',
      'PodDisruptionBudgets to prevent simultaneous eviction',
      'EBS is AZ-bound — StatefulSets cannot failover without data replication; use EFS for multi-AZ',
    ],
  },
  {
    id: 'arch-005',
    category: 'architecture',
    question: 'What are practical strategies for reducing EKS costs without sacrificing reliability?',
    answer: 'Use Karpenter with consolidation enabled to replace underutilized nodes with smaller ones automatically. Mix spot instances for stateless workloads (use multiple instance families for better spot availability). Right-size resource requests using VPA recommendations or metrics analysis — over-requesting wastes node capacity. Use Graviton (ARM) instances for 20-40% cost savings on compatible workloads. Schedule non-production clusters to scale down at night and weekends. Implement ResourceQuotas per namespace to prevent runaway resource consumption. Consider Savings Plans or Reserved Instances for baseline capacity. Use `topologySpreadConstraints` to avoid AZ imbalance that wastes capacity.',
    hint: 'Spot, right-sizing, consolidation, and Graviton are the biggest levers.',
    difficulty: 'normal',
    tags: ['cost', 'optimization', 'karpenter', 'spot'],
    keyPoints: [
      'Karpenter consolidation: replaces underutilized nodes with smaller ones',
      'Spot instances for stateless workloads (diversify instance families)',
      'Right-size requests with VPA or metrics — over-requesting wastes capacity',
      'Graviton instances: 20-40% cost savings for compatible workloads',
    ],
  },
  {
    id: 'arch-006',
    category: 'architecture',
    question: 'Design a Terraform multi-account structure using Terragrunt. How do you organize the repo and handle shared modules?',
    answer: 'Use a directory structure like: `live/{account}/{region}/{environment}/{component}/terragrunt.hcl` with shared configurations at each level. A root `terragrunt.hcl` configures the S3 backend with dynamic bucket/key paths based on directory position. Each account has its own state bucket. Shared Terraform modules live in a separate `modules/` repo with semantic versioning and are referenced by version tag in Terragrunt source blocks. Use `include` blocks for DRY backend/provider configuration and `dependency` blocks to pass outputs between components (e.g., VPC outputs to EKS). `generate` blocks create provider configurations with account-specific assume-role ARNs. This structure scales to hundreds of accounts while keeping each component independently plannable.',
    hint: 'Think about directory hierarchy that encodes account, region, and environment.',
    difficulty: 'expert',
    tags: ['terragrunt', 'multi-account', 'terraform', 'structure'],
    keyPoints: [
      'Directory structure: live/{account}/{region}/{env}/{component}/terragrunt.hcl',
      'Each account has its own state bucket',
      'Shared modules in separate repo with semantic versioning',
      'include blocks for DRY config, dependency blocks to pass outputs between components',
    ],
  },
  {
    id: 'arch-007',
    category: 'architecture',
    question: 'What should an incident response runbook for a production EKS cluster include, and how do you design it to be useful under pressure?',
    answer: 'A runbook should include: (1) symptom-based entry points (e.g., "pods not scheduling", "high error rate", "node NotReady") rather than a linear document. (2) Exact commands to run for diagnosis (kubectl, AWS CLI) with copy-paste-ready syntax. (3) Escalation matrix with names, roles, and contact methods. (4) Decision trees: "If X, do Y; if Z, do W." (5) Links to relevant dashboards, log queries, and architecture diagrams. (6) Rollback procedures for each deployment type. Design for stress: assume the reader is panicking at 3am, so use short sentences, numbered steps, and avoid walls of text. Store runbooks in a searchable system (Confluence, Notion) and test them during game days.',
    hint: 'Design for someone who is stressed at 3am — not for someone reading documentation casually.',
    difficulty: 'normal',
    tags: ['incident-response', 'runbooks', 'sre'],
    keyPoints: [
      'Symptom-based entry points (not linear) — "pods not scheduling", "node NotReady"',
      'Exact copy-paste commands for diagnosis',
      'Escalation matrix and decision trees',
      'Design for 3am stress: short sentences, numbered steps, no walls of text',
    ],
  },
  {
    id: 'arch-008',
    category: 'architecture',
    question: 'When does a service mesh (like Istio or Linkerd) make sense, and when is it over-engineering?',
    answer: 'A service mesh makes sense when you need: mutual TLS between all services without application changes, fine-grained traffic routing (canary at the network level, header-based routing), distributed tracing injection without SDK changes, or circuit breaking and retry policies applied uniformly. It is over-engineering when you have fewer than 10 services, when your team does not have the expertise to operate the mesh control plane, or when simpler solutions exist (e.g., ALB for traffic splitting, application-level retries). Istio adds significant operational complexity and resource overhead (sidecar proxies on every pod). Linkerd is lighter but has fewer features. Consider if your problems are actually solved by Kubernetes NetworkPolicies + Argo Rollouts first.',
    hint: 'What specific problems does a mesh solve that you cannot solve with simpler tools?',
    difficulty: 'hard',
    tags: ['service-mesh', 'istio', 'architecture'],
    keyPoints: [
      'Use mesh for: mTLS without app changes, header-based routing, tracing injection, circuit breaking',
      'Over-engineering: < 10 services, team lacks operational expertise',
      'Istio: complex, heavy sidecar overhead; Linkerd: lighter but fewer features',
      'Check if NetworkPolicies + Argo Rollouts solve your problem first',
    ],
  },
  {
    id: 'arch-009',
    category: 'architecture',
    question: 'Compare the platform team vs. product team ownership model for Kubernetes. Where should the boundary be?',
    answer: 'The platform team owns the cluster infrastructure: EKS cluster, node groups, networking (VPC CNI, ingress controllers), core add-ons (ArgoCD, monitoring stack, ESO), RBAC structure, and namespace provisioning. Product teams own their workloads: Deployment manifests, Helm charts/values, application-level monitoring, resource requests, and HPA configurations. The boundary is the namespace: platform team provisions namespaces with ResourceQuotas, LimitRanges, and NetworkPolicies; product teams deploy freely within their namespace. Common friction points: who owns the Ingress resource (shared ALB vs. per-team), who sets resource limits, and who responds to pod-level alerts. A good platform team provides self-service tooling (namespace request, ArgoCD project provisioning) rather than being a ticket queue.',
    hint: 'The namespace is usually the boundary — platform below, product above.',
    difficulty: 'normal',
    tags: ['platform-engineering', 'ownership', 'organization'],
    keyPoints: [
      'Platform: cluster infra, networking, add-ons, RBAC, namespace provisioning',
      'Product: Deployments, Helm values, HPA, resource requests',
      'Namespace is the boundary — platform provisions it with quotas + network policies',
      'Platform provides self-service tooling, not a ticket queue',
    ],
  },
  {
    id: 'arch-010',
    category: 'architecture',
    question: 'How do you test Infrastructure as Code? Describe a testing strategy at multiple levels.',
    answer: 'Level 1 — Static analysis: `terraform validate`, `tflint` for Terraform-specific linting, `checkov` or `tfsec` for security policy checks (e.g., S3 buckets must have encryption). Level 2 — Unit tests: Terratest (Go) or pytest with `terraform plan -out=plan.json` to assert that plan output contains expected resources without applying. Level 3 — Integration tests: apply to an ephemeral environment, run assertions (can I reach this endpoint? Does the RDS instance accept connections?), then destroy. Use Terratest or the `terraform test` framework (Terraform 1.6+). Level 4 — Policy as code: OPA/Rego or Sentinel policies in CI to block non-compliant plans. Run levels 1-2 on every PR, level 3 nightly or on merge to main, level 4 as a CI gate.',
    hint: 'Think about a testing pyramid: fast static checks at the base, slow integration tests at the top.',
    difficulty: 'hard',
    tags: ['testing', 'iac', 'terratest', 'policy'],
    keyPoints: [
      'L1 — Static: terraform validate, tflint, checkov/tfsec',
      'L2 — Unit: Terratest or plan assertions (no apply needed)',
      'L3 — Integration: apply to ephemeral env, assert, destroy',
      'L4 — Policy as code: OPA/Rego or Sentinel as CI gate',
    ],
  },

  // =============================================
  // OBSERVABILITY (5 questions)
  // =============================================
  {
    id: 'obs-001',
    category: 'observability',
    question: 'Compare the RED method and the USE method for monitoring. When do you apply each?',
    answer: 'RED (Rate, Errors, Duration) is for request-driven services — measure request rate, error rate, and latency distribution. It tells you how your users are experiencing the service. USE (Utilization, Saturation, Errors) is for infrastructure resources — measure how busy a resource is, how much queued work it has, and its error count. Apply RED to your API servers, web services, and microservice endpoints. Apply USE to your nodes (CPU utilization, memory saturation, disk errors), databases (connection pool utilization, query queue saturation), and network links. In practice, you need both: RED for the application layer and USE for the infrastructure layer underneath it.',
    hint: 'One is for services (user-facing); the other is for resources (infrastructure).',
    difficulty: 'normal',
    tags: ['red', 'use', 'monitoring', 'methodology'],
    keyPoints: [
      'RED: Rate, Errors, Duration — for request-driven services',
      'USE: Utilization, Saturation, Errors — for infrastructure resources',
      'RED for API/web services; USE for nodes, databases, network',
      'Need both: RED for app layer, USE for infra layer',
    ],
  },
  {
    id: 'obs-002',
    category: 'observability',
    question: 'What is the difference between Prometheus recording rules and alerting rules? Why would you use recording rules for dashboards?',
    answer: 'Recording rules pre-compute expensive PromQL queries on a schedule and store the result as a new time series. Alerting rules evaluate PromQL expressions and fire alerts when conditions are met (e.g., error rate > 5% for 5 minutes). Use recording rules for dashboard queries that aggregate across many time series — for example, `sum(rate(http_requests_total[5m])) by (service)` over 1000 pods. Without recording rules, every dashboard load re-evaluates this expensive query against raw data. With a recording rule, Prometheus computes it every 30 seconds and stores the result, making dashboard queries instant. Recording rules also enable you to create aggregated SLI metrics that alerting rules can reference efficiently.',
    hint: 'Recording rules trade storage for query speed by pre-computing results.',
    difficulty: 'normal',
    tags: ['prometheus', 'recording-rules', 'alerting', 'performance'],
    keyPoints: [
      'Recording rules: pre-compute expensive PromQL on a schedule, stored as time series',
      'Alerting rules: fire when expression condition is met',
      'Recording rules make dashboard queries instant instead of recomputing each load',
      'Enable efficient SLI metrics that alerting rules can reference',
    ],
  },
  {
    id: 'obs-003',
    category: 'observability',
    question: 'What is cardinality in Prometheus, and how can high cardinality cause an outage?',
    answer: 'Cardinality is the number of unique time series, determined by every unique combination of metric name and label values. A metric `http_requests_total{method, status, path, user_id}` with 10 methods, 20 statuses, 1000 paths, and 100K users creates 20 billion potential series. Prometheus stores each series in memory, so high cardinality causes OOM kills on the Prometheus server. Common causes: using user IDs, request IDs, or unbounded label values. Detect it with `prometheus_tsdb_head_series` metric and TSDB status page. Fix it by removing high-cardinality labels, using recording rules to aggregate, or using relabel_configs to drop or bucket label values before ingestion.',
    hint: 'Every unique label combination creates a new time series in memory.',
    difficulty: 'expert',
    tags: ['prometheus', 'cardinality', 'performance', 'troubleshooting'],
    keyPoints: [
      'Cardinality = number of unique time series = unique label value combinations',
      'High cardinality → OOM kills on Prometheus server',
      'Common culprits: user IDs, request IDs, unbounded label values',
      'Fix: remove high-cardinality labels, use relabel_configs to drop/bucket before ingestion',
    ],
  },
  {
    id: 'obs-004',
    category: 'observability',
    question: 'You are implementing distributed tracing in a microservices environment. How do you decide on a sampling strategy?',
    answer: 'Head-based sampling decides at the trace start whether to sample (e.g., 10% of all traces). It is simple but you might miss rare errors. Tail-based sampling collects all spans, then decides after the trace completes whether to keep it — keeping all error traces, high-latency traces, and sampling down successful/fast traces. Tail-based is more valuable but requires a collector with enough memory to buffer in-flight traces (OpenTelemetry Collector with the tail_sampling processor). A practical strategy: 100% sampling for errors and traces over p99 latency threshold, 1-5% for normal traffic. In production, start with head-based sampling at 10%, then add tail-based for error capture as your observability platform matures.',
    hint: 'The question is whether you decide to sample at the beginning or end of a trace.',
    difficulty: 'hard',
    tags: ['tracing', 'sampling', 'opentelemetry'],
    keyPoints: [
      'Head-based: decide at trace start — simple but may miss rare errors',
      'Tail-based: decide after trace completes — keeps all errors and slow traces',
      'Tail-based needs collector with buffer memory (OTel Collector + tail_sampling)',
      'Practical: 100% for errors + p99 violations, 1-5% for normal traffic',
    ],
  },
  {
    id: 'obs-005',
    category: 'observability',
    question: 'Define SLI, SLO, and SLA, and give a concrete example of each for an API service.',
    answer: 'SLI (Service Level Indicator) is a measurement: "the proportion of requests that return successfully in under 300ms" — calculated as `(successful_fast_requests / total_requests) * 100`. SLO (Service Level Objective) is an internal target: "99.9% of requests will succeed under 300ms over a 30-day rolling window." SLA (Service Level Agreement) is an external contract with consequences: "we guarantee 99.5% availability; if we breach this, the customer gets 10% credit." SLOs should be stricter than SLAs to give you an error budget buffer. The error budget (100% - SLO = 0.1% for a 99.9% SLO) is the amount of allowed unreliability, which you can "spend" on deployments, experiments, and maintenance.',
    hint: 'Indicator is what you measure, Objective is what you target, Agreement is what you promise externally.',
    difficulty: 'easy',
    tags: ['sli', 'slo', 'sla', 'sre'],
    keyPoints: [
      'SLI: the measurement (e.g., % of requests < 300ms)',
      'SLO: internal target (e.g., 99.9% over 30-day window)',
      'SLA: external contract with consequences (e.g., 99.5% with credit on breach)',
      'SLO stricter than SLA → error budget buffer',
    ],
  },
];
