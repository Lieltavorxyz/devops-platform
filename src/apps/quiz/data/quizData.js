export const categories = [
  {
    id: 'terraform',
    label: 'Terraform',
    icon: '🏗️',
    color: 'purple',
    description: 'State management, modules, drift, workspaces',
  },
  {
    id: 'kubernetes',
    label: 'Kubernetes',
    icon: '☸️',
    color: 'blue',
    description: 'Core concepts, scheduling, networking, troubleshooting',
  },
  {
    id: 'eks-aws',
    label: 'EKS & AWS',
    icon: '☁️',
    color: 'orange',
    description: 'Managed Kubernetes on AWS, networking, IAM integration',
  },
  {
    id: 'cicd-gitops',
    label: 'CI/CD & GitOps',
    icon: '🔄',
    color: 'green',
    description: 'ArgoCD, GitHub Actions, deployment strategies',
  },
  {
    id: 'architecture',
    label: 'Architecture',
    icon: '🏛️',
    color: 'teal',
    description: 'System design, multi-account strategy, platform engineering',
  },
  {
    id: 'observability',
    label: 'Observability',
    icon: '📊',
    color: 'yellow',
    description: 'Monitoring, alerting, SLOs, distributed tracing',
  },
];

// difficulty tiers: easy | normal | hard | expert

export const questions = [
  // =============================================
  // TERRAFORM (10 questions)
  // =============================================
  {
    id: 'tf-001',
    category: 'terraform',
    question: 'Your team stores Terraform state in S3. Two engineers run `terraform apply` at the same time — both plans succeed. What prevents state corruption?',
    options: [
      'DynamoDB provides state locking via a write-locked item per state file path',
      'S3 versioning automatically prevents concurrent writes to the same file',
      'Terraform acquires a file lock on the local `.terraform` directory',
      'AWS IAM policies can be configured to block concurrent apply runs',
    ],
    correctIndex: 0,
    answer: 'DynamoDB is used as a lock table for S3 backends. When you run plan or apply, Terraform acquires a lock via a DynamoDB item keyed by the state file path. If locking is misconfigured or missing, two simultaneous applies can write to the same state file, causing state corruption where resources exist in AWS but are missing from state (or vice versa). Recovery requires `terraform import` or manually editing the state file.',
    hint: 'Think about what DynamoDB does in the S3 backend configuration.',
    difficulty: 'normal',
    tags: ['state', 'locking', 's3', 'dynamodb'],
  },
  {
    id: 'tf-002',
    category: 'terraform',
    question: 'Why is storing secrets in Terraform state a security risk?',
    options: [
      'State files stored in S3 are publicly readable by default',
      'Terraform state stores all resource attributes in plaintext, including passwords and keys passed as resource arguments',
      'Terraform compresses state files which makes encryption impossible',
      'Provider credentials are written into the state file on every apply',
    ],
    correctIndex: 1,
    answer: 'Terraform state stores all resource attributes in plaintext JSON, including database passwords, API keys, and TLS private keys that were passed as resource arguments. Two mitigations: (1) enable server-side encryption on the S3 bucket (SSE-KMS) and restrict access via IAM policies, and (2) use a secrets manager (AWS Secrets Manager or Vault) so the secret value is a reference ID in state rather than the actual secret.',
    hint: 'Consider what gets written to the tfstate JSON when you create an RDS instance with a master password.',
    difficulty: 'normal',
    tags: ['state', 'security', 'secrets'],
  },
  {
    id: 'tf-003',
    category: 'terraform',
    question: 'When should you use Terraform workspaces vs. a directory-per-environment structure?',
    options: [
      'Always use workspaces — they provide better state isolation than directories',
      'Workspaces for structurally identical environments differing only by variable values; directories when environments diverge or need separate access controls',
      'Directory-per-env only when using Terraform Cloud; workspaces for self-managed state',
      'Workspaces when you have more than 3 environments; directories for fewer',
    ],
    correctIndex: 1,
    answer: 'Workspaces are useful when environments are structurally identical and only differ by variable values (instance size, replica count). Directory-per-environment is better when environments diverge in resources or you want independent state files with separate access controls. The main risk of workspaces: all environments share the same backend configuration, so an IAM misconfiguration could expose prod state. Running `terraform workspace select prod` followed by a careless apply in what you thought was dev is a real incident pattern.',
    hint: 'Think about state isolation and what happens when a developer forgets which workspace is active.',
    difficulty: 'hard',
    tags: ['workspaces', 'environments', 'state'],
  },
  {
    id: 'tf-004',
    category: 'terraform',
    question: 'You publish a shared Terraform module. A consumer pins `version = "~> 2.1"`. You release v2.3.0 with a new required variable. What happens?',
    options: [
      'Adding a required variable is a breaking change — should have been released as v3.0.0',
      'The plan shows a warning but still succeeds; Terraform infers a default value',
      'The consumer must update their version constraint to `~> 2.3` to pick up the change',
      'Adding a required variable is a minor change since it only extends the interface',
    ],
    correctIndex: 0,
    answer: 'The consumer\'s next `terraform init -upgrade` will pull v2.3.0, and their plan will fail because the new required variable has no default and is not set in their module call. Adding a required variable is a breaking change and should have been released as v3.0.0 per semver. Alternatively, add the variable with a sensible default to keep it backward-compatible within the 2.x line.',
    hint: 'Consider what semver means for Terraform module interfaces.',
    difficulty: 'normal',
    tags: ['modules', 'versioning', 'semver'],
  },
  {
    id: 'tf-005',
    category: 'terraform',
    question: 'Compare the `terraform import` CLI command with the `import` block (Terraform 1.5+). Which is better for bulk imports?',
    options: [
      'CLI import is always preferred — it immediately updates state without running plan',
      '`import` block: declarative, visible in `terraform plan`, reviewable in PRs — better for bulk imports and CI/CD',
      'They are identical; `import` blocks are just syntactic sugar around the CLI command',
      'CLI import shows a plan preview; `import` block modifies state directly',
    ],
    correctIndex: 1,
    answer: '`terraform import` is a CLI command that modifies state directly — you must manually write the resource block first, then run the import, then iteratively fix the config until plan shows no diff. The `import` block (Terraform 1.5+) is declarative: you add an `import { to = ... ; id = "..." }` block in HCL, and `terraform plan` shows the proposed import. Prefer import blocks for bulk imports and CI/CD pipelines because they are reviewable in PRs and repeatable. Use CLI import for one-off resources or pre-1.5 Terraform.',
    hint: 'One approach is imperative and modifies state directly; the other is declarative and plan-visible.',
    difficulty: 'hard',
    tags: ['import', 'state', 'migration'],
  },
  {
    id: 'tf-006',
    category: 'terraform',
    question: 'How do you build an automated drift detection pipeline in Terraform?',
    options: [
      '`terraform refresh` auto-detects and fixes drift when run on a schedule',
      '`terraform plan -detailed-exitcode` on a schedule — exit code 2 means drift detected, trigger an alert',
      '`terraform validate` checks if resources have changed since last apply',
      'AWS Config is the only reliable way to detect Terraform drift',
    ],
    correctIndex: 1,
    answer: 'Terraform detects drift by running `terraform plan`, which compares the state file against real infrastructure via provider API calls. Its limitation: it only checks managed resources — resources added outside Terraform are invisible. A drift detection pipeline runs `terraform plan -detailed-exitcode` on a schedule (e.g., nightly cron). Exit code 2 means drift detected, which triggers an alert. Beware: some resources have frequently-changing attributes (ECS task definition revisions) that produce noisy false positives.',
    hint: 'Think about what `terraform plan -detailed-exitcode` returns and how you would automate it.',
    difficulty: 'hard',
    tags: ['drift', 'ci-cd', 'automation'],
  },
  {
    id: 'tf-007',
    category: 'terraform',
    question: 'When is `depends_on` actually necessary in Terraform?',
    options: [
      'Whenever two resources are in the same module and one creates before the other',
      'When the dependency exists but is not expressed through attribute references — e.g., an IAM policy attachment that must exist before an app boots',
      'For all cross-module dependencies because Terraform cannot infer them automatically',
      'Whenever you need ordered execution between any two resources',
    ],
    correctIndex: 1,
    answer: 'Implicit dependencies are created automatically when one resource references another\'s attribute (e.g., `subnet_id = aws_subnet.main.id`). Terraform builds a DAG from these references and orders operations correctly. `depends_on` is needed when the dependency exists but is not expressed through attribute references — for example, an app server that needs an IAM policy attachment to exist before it boots, but does not reference the policy ARN in its config. Overusing `depends_on` forces serial execution and is an anti-pattern.',
    hint: 'When does Terraform know about a dependency automatically vs. when do you have to tell it?',
    difficulty: 'easy',
    tags: ['dependencies', 'dag', 'depends_on'],
  },
  {
    id: 'tf-008',
    category: 'terraform',
    question: 'You use `count` to create 5 IAM users. You remove the 3rd user from the list. What happens?',
    options: [
      'Only the 3rd user is destroyed; others are unaffected',
      'Terraform destroys and recreates users 3, 4, and 5 because their numeric indexes shift',
      'Terraform shows a plan error requiring you to use `terraform state mv` first',
      'count and for_each behave identically for this use case',
    ],
    correctIndex: 1,
    answer: 'With `count`, resources are identified by numeric index (aws_iam_user.users[0], [1], etc.). If you remove a user from the middle of the list, every subsequent resource shifts index and Terraform wants to destroy and recreate them. `for_each` uses map keys or set values as identifiers (aws_iam_user.users["alice"]), so removing one user only affects that specific resource. Use `for_each` whenever the items have a meaningful identity.',
    hint: 'Consider what happens to resource addresses when you remove the second item from a list of five.',
    difficulty: 'normal',
    tags: ['count', 'for_each', 'iteration'],
  },
  {
    id: 'tf-009',
    category: 'terraform',
    question: 'What is the difference between `locals` and `variables` in Terraform?',
    options: [
      'Locals are for sensitive values; variables are for non-sensitive configuration',
      'Locals are computed values internal to the module; variables are input parameters set by the caller',
      'Variables and locals are equivalent; locals just cannot be overridden at plan time with `-var`',
      'Variables should be used for all derived values to improve module readability',
    ],
    correctIndex: 1,
    answer: 'Variables are input parameters — they are set by the caller (via tfvars, CLI flags, or module arguments) and define the module\'s interface. Locals are computed values internal to the module that cannot be set externally. Use locals for derived values like `local.env_prefix = "${var.project}-${var.environment}"` that you reference in multiple resources. A common anti-pattern: creating a variable for something that should never be overridden by the caller.',
    hint: 'Think about who sets the value — the module consumer or the module author.',
    difficulty: 'easy',
    tags: ['locals', 'variables', 'modules'],
  },
  {
    id: 'tf-010',
    category: 'terraform',
    question: 'A provider upgrade breaks your Terraform apply. What is the role of `.terraform.lock.hcl`?',
    options: [
      '.terraform.lock.hcl should be gitignored so each team member can use their preferred provider version',
      '.terraform.lock.hcl records the exact provider version and hashes after `terraform init` — commit it to version control',
      'Version constraints in `required_providers` are sufficient; the lock file is optional',
      'The lock file only matters for providers sourced from private registries',
    ],
    correctIndex: 1,
    answer: 'Pin provider versions in `required_providers` using `version = "~> 5.0"` to allow patch updates but not major version jumps. The `.terraform.lock.hcl` file records the exact provider version and its hashes after `terraform init`, ensuring all team members and CI use the same binary. Commit the lock file to version control. To upgrade, run `terraform init -upgrade`. Without pinning, `terraform init` can pull the latest provider version, which may include breaking changes.',
    hint: 'There are two layers: the version constraint in HCL and the lock file that records exact versions.',
    difficulty: 'easy',
    tags: ['providers', 'versioning', 'lockfile'],
  },

  // =============================================
  // KUBERNETES (15 questions)
  // =============================================
  {
    id: 'k8s-001',
    category: 'kubernetes',
    question: 'A pod has `requests.memory: 256Mi` and `limits.memory: 512Mi`. What QoS class is it assigned, and what happens first under memory pressure?',
    options: [
      'Guaranteed — evicted last because it has both requests and limits defined',
      'Burstable — evicted before Guaranteed pods; if usage exceeds 512Mi the kernel OOM-kills it immediately',
      'BestEffort — it has limits but no CPU requests, so it gets the lowest QoS class',
      'Burstable — evicted before BestEffort pods because it has defined limits',
    ],
    correctIndex: 1,
    answer: 'This pod gets Burstable QoS because requests and limits are set but not equal. Under memory pressure, kubelet evicts in order: BestEffort first (no requests/limits), then Burstable (sorted by how much they exceed their request), then Guaranteed (requests == limits) last. If this pod is using 400Mi (above its 256Mi request), it is a prime eviction candidate. If it exceeds 512Mi, the kernel OOM killer terminates the container immediately regardless of node pressure.',
    hint: 'QoS class depends on the relationship between requests and limits.',
    difficulty: 'normal',
    tags: ['qos', 'resources', 'eviction'],
  },
  {
    id: 'k8s-002',
    category: 'kubernetes',
    question: 'You need to autoscale a workload based on SQS queue depth. Why is standard HPA insufficient and what solves this?',
    options: [
      'HPA can scale on SQS natively via CloudWatch integration — no extra tooling needed',
      'KEDA: built-in SQS scaler, can scale to zero replicas; HPA natively supports only CPU and memory',
      'KEDA replaces HPA entirely — you cannot use both at the same time',
      'Use a custom metrics adapter with HPA; KEDA only works with Kafka and Redis',
    ],
    correctIndex: 1,
    answer: 'HPA natively supports CPU and memory metrics, and can use custom metrics via the metrics API, but you need to deploy and configure a metrics adapter (like Prometheus Adapter) to expose SQS queue depth. KEDA simplifies this with built-in scalers for AWS SQS and 50+ other event sources. KEDA also scales to zero replicas — HPA minimum is 1. KEDA installs its own metrics adapter and feeds the metrics to HPA under the hood, so it extends rather than replaces HPA.',
    hint: 'Consider what metric sources HPA supports natively vs. what KEDA brings.',
    difficulty: 'normal',
    tags: ['hpa', 'keda', 'autoscaling'],
  },
  {
    id: 'k8s-003',
    category: 'kubernetes',
    question: 'Design a least-privilege RBAC setup for a CI/CD service account that deploys only to the "staging" namespace. What is the critical resource choice?',
    options: [
      'ClusterRole with a namespaceSelector targeting staging only',
      'Role (not ClusterRole) scoped to the staging namespace + RoleBinding; no wildcards on verbs or resources',
      'ServiceAccount with a ClusterRoleBinding using an overly restrictive cluster policy',
      'Role + ClusterRoleBinding — ClusterRoleBinding is needed to apply the Role across namespaces',
    ],
    correctIndex: 1,
    answer: 'Create a ServiceAccount in the staging namespace, a Role (not ClusterRole) scoped to that namespace with verbs like get, list, watch, create, update, patch on deployments, services, configmaps, secrets. Bind the Role to the ServiceAccount with a RoleBinding. Using a Role instead of ClusterRole ensures the permissions cannot leak to other namespaces. Avoid wildcards on verbs or resources — enumerate exactly what the CI/CD pipeline needs.',
    hint: 'Role vs ClusterRole is the key namespace isolation decision.',
    difficulty: 'normal',
    tags: ['rbac', 'security', 'service-account'],
  },
  {
    id: 'k8s-004',
    category: 'kubernetes',
    question: 'You want to ensure at least 80% of pods remain available during voluntary disruptions like node drains. What do you configure?',
    options: [
      'Set `maxUnavailable: 20%` on the Deployment rollout strategy',
      'PodDisruptionBudget with `minAvailable: 80%` — eviction API respects it during voluntary disruptions',
      'ReplicaSet with `minReadySeconds` ensures pods stay available during node drains',
      'PodDisruptionBudget protects against both voluntary (drains) and involuntary (node crash) disruptions equally',
    ],
    correctIndex: 1,
    answer: 'Create a PodDisruptionBudget (PDB) with `minAvailable: 80%` (or equivalently `maxUnavailable: 20%`) and a selector matching the deployment\'s pods. During voluntary disruptions like node drains, `kubectl drain`, or cluster autoscaler scale-down, the eviction API respects the PDB and blocks eviction if it would violate the budget. PDBs do NOT protect against involuntary disruptions (node crashes, OOM kills). If you set minAvailable too high (e.g., 100% with only 2 replicas), node drains will hang indefinitely.',
    hint: 'This is a specific Kubernetes resource designed for voluntary disruption protection.',
    difficulty: 'normal',
    tags: ['pdb', 'availability', 'disruptions'],
  },
  {
    id: 'k8s-005',
    category: 'kubernetes',
    question: 'You set a liveness probe with a 5-second timeout on an app that takes 60 seconds to start. What happens?',
    options: [
      'The pod starts normally; liveness probes are disabled during the initial boot period',
      'The container enters CrashLoopBackOff — kubelet keeps killing it before it finishes starting',
      'Kubernetes delays the liveness probe until the readiness probe first succeeds',
      'The pod shows as Degraded but keeps running until it finishes starting',
    ],
    correctIndex: 1,
    answer: 'Readiness probes gate traffic — failing removes the pod from Service endpoints but does not restart it. Liveness probes detect deadlocks — failing causes kubelet to restart the container. The low liveness timeout causes kubelet to repeatedly kill the container before it finishes starting, entering CrashLoopBackOff. This is exactly what startup probes solve: they disable liveness and readiness checks until the app finishes booting (up to failureThreshold × periodSeconds).',
    hint: 'Each probe type has a different consequence for failure.',
    difficulty: 'normal',
    tags: ['probes', 'health-checks', 'troubleshooting'],
  },
  {
    id: 'k8s-006',
    category: 'kubernetes',
    question: 'GPU nodes are tainted `nvidia.com/gpu=true:NoSchedule` and labeled `gpu-type: a100`. You want GPU pods to schedule ONLY on A100 nodes. What do you need?',
    options: [
      'Only a nodeSelector for `gpu-type: a100` — nodeSelector overrides taints automatically',
      'A toleration to allow scheduling on tainted nodes + a nodeSelector/affinity to target A100 specifically',
      'Only a toleration — it both allows scheduling and attracts pods to the tainted node',
      'A taint on the pod matching the node taint; tolerations are only for nodes',
    ],
    correctIndex: 1,
    answer: 'nodeSelector is a simple key-value match that constrains pods to labeled nodes. Node affinity is more expressive with operators and soft/hard preferences. Tolerations allow pods to be scheduled on tainted nodes but do not attract them there. For GPU workloads you need: a toleration (to allow scheduling on tainted GPU nodes) AND a nodeSelector or affinity (to target A100 specifically). Without the toleration the pod stays Pending; without the affinity it could land on any node that happens to tolerate the taint.',
    hint: 'Taints + tolerations control admission; affinity/nodeSelector control attraction.',
    difficulty: 'hard',
    tags: ['scheduling', 'affinity', 'taints', 'tolerations'],
  },
  {
    id: 'k8s-007',
    category: 'kubernetes',
    question: 'Are Kubernetes Secrets actually secure by default?',
    options: [
      'Yes — etcd encrypts Secrets automatically using AES-256',
      'No — Secrets are only base64-encoded; anyone with RBAC read access can decode them trivially',
      'Yes — Secrets are stored encrypted in S3 when using EKS',
      'Partially — Secrets are encrypted in transit but not at rest by default',
    ],
    correctIndex: 1,
    answer: 'Secrets are only base64-encoded by default, not encrypted — anyone with RBAC read access to Secrets in a namespace can decode them instantly. To make Secrets actually secure: enable encryption at rest (EncryptionConfiguration with a KMS provider), restrict RBAC access to Secrets, and consider using External Secrets Operator (ESO) that fetches secrets from Vault or AWS Secrets Manager at runtime rather than storing them in etcd.',
    hint: 'Base64 encoding is not encryption.',
    difficulty: 'easy',
    tags: ['configmap', 'secrets', 'security'],
  },
  {
    id: 'k8s-008',
    category: 'kubernetes',
    question: 'You need to run a log collection agent on every node in the cluster. Which workload controller do you use?',
    options: [
      'Deployment with `replicas` set to the number of nodes',
      'DaemonSet — ensures exactly one pod per node, automatically adds/removes pods as nodes join or drain',
      'StatefulSet with `hostNetwork: true` for node-level log access',
      'ReplicaSet scaled to match node count via Cluster Autoscaler',
    ],
    correctIndex: 1,
    answer: 'DaemonSets ensure exactly one pod runs on every (or selected) node. For a logging agent, use a DaemonSet because you need one agent per node to collect that node\'s logs. DaemonSets automatically add pods when new nodes join the cluster and remove them when nodes are drained — exactly the behavior you want for node-level agents. Deployments manage stateless replicas; StatefulSets provide stable identities for databases; neither guarantees one-pod-per-node.',
    hint: 'Which controller guarantees one pod per node?',
    difficulty: 'easy',
    tags: ['deployment', 'statefulset', 'daemonset', 'workloads'],
  },
  {
    id: 'k8s-009',
    category: 'kubernetes',
    question: 'How do you implement a default-deny network policy so all pods in a namespace are isolated by default?',
    options: [
      'Apply a NetworkPolicy with `action: deny` to the namespace',
      'NetworkPolicy with `podSelector: {}` + `policyTypes: [Ingress, Egress]` and no ingress/egress rules = deny all',
      '`podSelector: {}` with `namespaceSelector: {}` denies all cross-namespace traffic only',
      'Set `networkIsolation: true` on the Namespace object',
    ],
    correctIndex: 1,
    answer: 'Create a NetworkPolicy with `podSelector: {}` (matches all pods in the namespace) and empty ingress and egress rules (`policyTypes: ["Ingress", "Egress"]` with no ingress/egress blocks). This denies all traffic to and from every pod in the namespace. Then add policies that allow specific traffic — NetworkPolicies are additive, so each new policy opens specific paths without revoking the default-deny. Note: requires a CNI that enforces NetworkPolicy (Calico, Cilium — not default kubenet).',
    hint: 'An empty podSelector matches everything; empty rules deny everything.',
    difficulty: 'hard',
    tags: ['network-policy', 'security', 'zero-trust'],
  },
  {
    id: 'k8s-010',
    category: 'kubernetes',
    question: 'A node is under memory pressure. What is the eviction priority order?',
    options: [
      'Guaranteed → Burstable → BestEffort (most resource-heavy evicted first)',
      'BestEffort first, then Burstable (sorted by excess usage over request), then Guaranteed last',
      'All QoS classes have equal eviction priority; oldest pods are evicted first',
      'Burstable first because they have defined limits that can be exceeded',
    ],
    correctIndex: 1,
    answer: 'When a node hits a resource threshold, kubelet evicts based on QoS class: BestEffort pods (no requests/limits) first, then Burstable pods sorted by how much they exceed their requests relative to node capacity, then Guaranteed pods last. This is why setting accurate requests is critical — a pod requesting 100Mi but using 900Mi is a high-priority eviction target. PriorityClass also factors in within the same QoS class.',
    hint: 'QoS class is the primary sort key.',
    difficulty: 'hard',
    tags: ['eviction', 'qos', 'scheduling'],
  },
  {
    id: 'k8s-011',
    category: 'kubernetes',
    question: 'A pod is in CrashLoopBackOff. What is your first debugging step?',
    options: [
      '`kubectl exec` into the container to inspect running processes',
      '`kubectl describe pod` for Events + `kubectl logs --previous` for the last crash output',
      '`kubectl get events --sort-by=age` is the primary debugging tool for crashed pods',
      'Check node-level logs with `journalctl` to find the container crash reason',
    ],
    correctIndex: 1,
    answer: 'First, `kubectl describe pod <name>` to check the Events section for OOMKilled, image pull errors, or failed mounts. Then `kubectl logs <pod> --previous` to see the last container\'s stdout/stderr before it crashed. Check for application errors (stack trace in logs), misconfigured command/args, missing environment variables, or a failed health check killing the container. If logs are empty, the process crashes before writing output — try running the image locally with `docker run`. Delete the pod to reset the exponential backoff timer.',
    hint: 'Start with describe, then previous logs, then check events for the root cause.',
    difficulty: 'easy',
    tags: ['troubleshooting', 'crashloop', 'debugging'],
  },
  {
    id: 'k8s-012',
    category: 'kubernetes',
    question: 'What is the difference between a container being OOMKilled vs. CPU throttled?',
    options: [
      'OOMKilled slows the container; CPU throttling kills it and restarts it',
      'OOMKilled: process killed by kernel (exit code 137); CPU throttling: container slowed by CFS scheduler but keeps running',
      'Both restart the container — OOMKilled for memory, throttling for CPU overuse',
      'CPU throttling kills the container; OOMKilled logs a warning but the process continues',
    ],
    correctIndex: 1,
    answer: 'OOMKilled means the container exceeded its memory limit and the kernel killed the process (exit code 137) — visible in `kubectl describe pod` as `reason: OOMKilled`. CPU throttling means the container is using more CPU than its limit and the kernel CFS scheduler forces it to wait. The container stays running but becomes slow. Detect throttling via `container_cpu_cfs_throttled_periods_total` Prometheus metric. Recommendation: set CPU requests but not CPU limits to avoid unnecessary throttling.',
    hint: 'One kills the process; the other slows it down.',
    difficulty: 'normal',
    tags: ['oomkilled', 'throttling', 'resources', 'troubleshooting'],
  },
  {
    id: 'k8s-013',
    category: 'kubernetes',
    question: 'What does a Kubernetes namespace actually isolate?',
    options: [
      'Namespaces provide complete network isolation — pods cannot talk across namespaces',
      'Namespaces isolate names and RBAC scopes, but NOT network traffic by default — pods can freely talk across namespaces',
      'Namespaces isolate node resources — pods from different namespaces cannot share the same node',
      'Namespaces are a security boundary equivalent to separate clusters',
    ],
    correctIndex: 1,
    answer: 'Namespaces provide a scope for names, RBAC boundaries (Roles are namespace-scoped), and resource quotas. However, namespaces do NOT isolate network traffic by default (pods can talk across namespaces freely without NetworkPolicies), do not isolate node resources (pods from different namespaces share the same node), and do not isolate cluster-scoped resources (ClusterRoles, Nodes, PersistentVolumes). True multi-tenancy requires NetworkPolicies, ResourceQuotas, and potentially separate clusters.',
    hint: 'Namespaces are a soft boundary, not a security boundary, unless you add policies.',
    difficulty: 'normal',
    tags: ['namespaces', 'isolation', 'multi-tenancy'],
  },
  {
    id: 'k8s-014',
    category: 'kubernetes',
    question: 'Why is IRSA (IAM Roles for Service Accounts) preferred over attaching an IAM role to the EC2 node?',
    options: [
      'Node IAM roles automatically rotate; IRSA credentials are static and need manual rotation',
      'Node IAM: every pod on the node shares the same credentials — a compromised pod can access the node\'s full IAM role',
      'IRSA and node IAM roles are equivalent in security; IRSA just adds operational complexity',
      'Node IAM requires IMDSv1; IRSA requires IMDSv2, which is more secure',
    ],
    correctIndex: 1,
    answer: 'With node IAM roles, every pod on that node can access the same IAM permissions by hitting the instance metadata service (169.254.169.254). A compromised pod can escalate privileges by assuming the node role, which typically has broad permissions (ECR pull, CloudWatch, etc.). IRSA scopes credentials to individual Kubernetes ServiceAccounts. Additionally, to fully mitigate node role risk, enforce IMDSv2 with hop limit 1 to prevent pods from reaching the metadata service.',
    hint: 'Think about the granularity: per-node vs. per-pod IAM permissions.',
    difficulty: 'hard',
    tags: ['irsa', 'iam', 'eks', 'security'],
  },
  {
    id: 'k8s-015',
    category: 'kubernetes',
    question: 'When would you deliberately choose the Recreate deployment strategy over RollingUpdate?',
    options: [
      'Recreate is preferred for stateless services to prevent two versions serving traffic simultaneously',
      'Recreate is the default strategy; RollingUpdate must be explicitly configured',
      'When two versions cannot run simultaneously — e.g., exclusive RWO PersistentVolume or database migration runner',
      'When `maxUnavailable: 0` is required; Recreate is the only way to guarantee zero downtime',
    ],
    correctIndex: 2,
    answer: 'RollingUpdate gradually replaces old pods with new ones, ensuring zero-downtime. Recreate kills all old pods before creating new ones, causing downtime. Choose Recreate when your application cannot tolerate two versions running simultaneously — for example, a database migration runner, a workload with exclusive access to a volume (RWO PersistentVolume), or a legacy app where running two versions causes data corruption. RollingUpdate is the default and is preferred for stateless web services.',
    hint: 'When would running old and new versions simultaneously cause problems?',
    difficulty: 'easy',
    tags: ['deployment', 'strategy', 'rolling-update'],
  },

  // =============================================
  // EKS & AWS (10 questions)
  // =============================================
  {
    id: 'eks-001',
    category: 'eks-aws',
    question: 'You want no node management overhead and are running batch jobs. Which EKS compute option fits, and what is its key limitation?',
    options: [
      'Managed node groups — AWS handles AMI updates and draining automatically',
      'Fargate profiles — no nodes to manage, but no DaemonSets, no EBS volumes, and higher per-pod cost',
      'Self-managed node groups — full AMI control for batch job optimization',
      'Fargate profiles — supports DaemonSets and EBS through a special Fargate adapter',
    ],
    correctIndex: 1,
    answer: 'Fargate profiles run pods without you managing nodes at all — good for burst workloads and batch jobs. Key limitations: no DaemonSets (you cannot run node agents like Fluent Bit), no EBS volumes (only EFS), higher per-pod cost than EC2 nodes, and slower pod startup. Managed node groups handle AMI updates, provisioning, and graceful draining — best for most production workloads. Self-managed node groups give full AMI control for custom drivers, GPU nodes, or Windows.',
    hint: 'Think about the tradeoff between zero node management and feature limitations.',
    difficulty: 'normal',
    tags: ['node-groups', 'fargate', 'eks'],
  },
  {
    id: 'eks-002',
    category: 'eks-aws',
    question: 'What is the key architectural difference between Karpenter and Cluster Autoscaler?',
    options: [
      'Karpenter uses ASGs but with a more intelligent scaling algorithm than CA',
      'Karpenter provisions EC2 instances directly via EC2 Fleet API, bypassing ASGs entirely; CA only adjusts ASG desired count',
      'Cluster Autoscaler is faster for scale-up; Karpenter is better for scale-down',
      'Karpenter only works with managed node groups; CA works with any ASG type',
    ],
    correctIndex: 1,
    answer: 'Cluster Autoscaler works with ASGs — it adjusts the desired count of pre-defined node groups, requiring separate ASGs for different instance types. Karpenter provisions nodes directly via EC2 Fleet API, bypassing ASGs entirely. It evaluates pending pod requirements and selects optimal instance types in real-time. Karpenter is faster (30-60 seconds vs. minutes for CA), handles heterogeneous workloads better, supports consolidation (replacing underutilized nodes), and diversifies spot allocation across instance families automatically.',
    hint: 'One works through ASGs; the other provisions EC2 instances directly.',
    difficulty: 'normal',
    tags: ['karpenter', 'cluster-autoscaler', 'scaling'],
  },
  {
    id: 'eks-003',
    category: 'eks-aws',
    question: 'Why is AWS moving from the `aws-auth` ConfigMap to EKS Access Entries?',
    options: [
      'aws-auth supports more IAM principal types than Access Entries',
      'aws-auth lives inside the cluster — delete or corrupt it and you lose all cluster access; Access Entries are managed via the EKS API outside the cluster',
      'Access Entries only work with Terraform; aws-auth supports all tools',
      'aws-auth is encrypted in etcd; Access Entries are stored in plaintext in DynamoDB',
    ],
    correctIndex: 1,
    answer: 'The aws-auth ConfigMap maps IAM principals to K8s RBAC groups, but it lives in kube-system as a regular ConfigMap. If it gets corrupted or deleted, you can lock everyone out of the cluster. EKS Access Entries (GA 2024) manage the same mappings via the EKS API, outside the cluster — manageable via Terraform/CloudFormation, auditable via CloudTrail, and cannot be accidentally deleted by a kubectl command. AWS is moving to Access Entries to eliminate this single-point-of-failure risk.',
    hint: 'One is a Kubernetes resource inside the cluster; the other is an AWS API resource outside it.',
    difficulty: 'hard',
    tags: ['aws-auth', 'access-entries', 'iam', 'security'],
  },
  {
    id: 'eks-004',
    category: 'eks-aws',
    question: 'You customized the CoreDNS ConfigMap, then upgraded the EKS managed add-on. Your custom forward rules are gone. Why?',
    options: [
      'CoreDNS add-on upgrades always reset to factory defaults; you must reconfigure after every upgrade',
      'EKS managed add-ons can overwrite manual customizations — use the `configurationValues` field to pass customizations through the managed add-on API',
      'You must delete the CoreDNS ConfigMap before every add-on upgrade or it will conflict',
      'Custom CoreDNS rules must be stored in a Secret, not a ConfigMap, to survive upgrades',
    ],
    correctIndex: 1,
    answer: 'EKS managed add-ons can overwrite custom configurations you made. If you customized the CoreDNS ConfigMap or modified the deployment (resource limits, replicas), an add-on update may revert those changes. The fix: use the `configurationValues` field in the add-on configuration to pass customizations through the managed add-on API. Always check the version compatibility matrix before upgrading. The `PRESERVE` conflict resolution strategy can help retain custom settings.',
    hint: 'Think about what happens to manual changes when AWS reconciles the add-on.',
    difficulty: 'hard',
    tags: ['add-ons', 'coredns', 'upgrades'],
  },
  {
    id: 'eks-005',
    category: 'eks-aws',
    question: 'Pods are stuck in ContainerCreating because the subnet ran out of IPs. What is the fastest fix without subnet recreation?',
    options: [
      'Reduce pod memory requests so fewer pods run per node',
      'Enable prefix delegation (`ENABLE_PREFIX_DELEGATION=true`) on VPC CNI — assigns /28 prefixes, giving 16x more pod IPs per ENI',
      'Increase the pod CIDR range in the EKS cluster configuration panel',
      'Enable IP aliasing to share VPC IPs between pods on the same node',
    ],
    correctIndex: 1,
    answer: 'The AWS VPC CNI assigns real VPC IP addresses to each pod. When the subnet runs out, new pods cannot be scheduled (ContainerCreating). Fastest fix without subnet recreation: enable prefix delegation (`ENABLE_PREFIX_DELEGATION=true`) which assigns /28 prefixes to node ENIs instead of individual IPs, giving 16x more pod IPs per ENI. Other options: add secondary CIDR ranges (100.64.0.0/16), tune `WARM_IP_TARGET`/`MINIMUM_IP_TARGET`, or use larger subnets for new node groups.',
    hint: 'Each pod gets a real VPC IP — what handles multiplying the IPs available per ENI?',
    difficulty: 'expert',
    tags: ['vpc-cni', 'networking', 'ip-exhaustion'],
  },
  {
    id: 'eks-006',
    category: 'eks-aws',
    question: 'What is the security risk of using a node IAM instance profile for all pod workloads?',
    options: [
      'Node IAM roles expire after 1 hour; IRSA credentials have longer TTLs',
      'Any pod on the node can hit the instance metadata service (169.254.169.254) and assume the full node IAM role',
      'Node IAM roles cannot be scoped to specific namespaces, making RBAC ineffective',
      'AWS does not recommend node IAM roles for EKS; only IRSA is officially supported',
    ],
    correctIndex: 1,
    answer: 'With node IAM roles, every pod on that node can access the same IAM permissions by hitting the instance metadata service (169.254.169.254). A compromised pod can escalate privileges by assuming the node role, which typically has broad permissions (ECR pull, CloudWatch, etc.). IRSA scopes credentials to individual Kubernetes ServiceAccounts — a pod in namespace A cannot access credentials meant for namespace B. Mitigate further by enforcing IMDSv2 with hop limit 1 to block pods from reaching the metadata service.',
    hint: 'Consider what happens when any pod on a node can reach the metadata service.',
    difficulty: 'normal',
    tags: ['irsa', 'iam', 'security', 'metadata'],
  },
  {
    id: 'eks-007',
    category: 'eks-aws',
    question: 'Which EKS control plane log type is essential for security incident investigation?',
    options: [
      'controllerManager — captures all pod scheduling decisions',
      'audit — records every API call with user identity, timestamp, and action',
      'scheduler — shows why pods were placed on specific nodes',
      'authenticator — sufficient for all security investigations',
    ],
    correctIndex: 1,
    answer: 'EKS supports five log types sent to CloudWatch: api, audit, authenticator, controllerManager, and scheduler. Always enable audit logs — they record every API call with user identity, critical for incident investigation (who deleted that deployment? who modified RBAC?). Authenticator logs help debug RBAC/IAM 403 issues. API server logs help debug webhook failures. These logs can be expensive at scale — set retention policies and use CloudWatch Insights for querying.',
    hint: 'Which log type records who did what to which resource?',
    difficulty: 'normal',
    tags: ['logging', 'cloudwatch', 'security', 'troubleshooting'],
  },
  {
    id: 'eks-008',
    category: 'eks-aws',
    question: 'When should you use an NLB instead of an ALB for exposing services in EKS?',
    options: [
      'Always prefer NLB — it is newer and supports more protocols than ALB',
      'NLB for TCP/UDP, gRPC with TLS passthrough, static IPs, or ultra-low latency; ALB for HTTP routing rules and WAF',
      'NLB when using Fargate; ALB when using managed node groups',
      'ALB and NLB can both handle gRPC with the same Ingress annotation',
    ],
    correctIndex: 1,
    answer: 'ALB (via AWS Load Balancer Controller) operates at Layer 7 — it supports path-based and host-based routing, HTTP/2, WebSockets, WAF integration, and can route to multiple services from a single ALB using Ingress rules. NLB operates at Layer 4 — it handles raw TCP/UDP, provides static IPs, supports TLS passthrough, has lower latency, and preserves client source IPs. Use ALB for HTTP microservices with routing rules and WAF. Use NLB for gRPC (with TLS passthrough), non-HTTP protocols, or when you need static IPs.',
    hint: 'Layer 7 (routing logic) vs Layer 4 (raw protocol).',
    difficulty: 'normal',
    tags: ['alb', 'nlb', 'ingress', 'networking'],
  },
  {
    id: 'eks-009',
    category: 'eks-aws',
    question: 'What is the correct order of operations for an EKS cluster version upgrade?',
    options: [
      'Upgrade node groups first to validate compatibility before touching the control plane',
      'Control plane → managed add-ons → node groups; never the reverse',
      'Add-ons first, then control plane, then node groups to minimize API incompatibility',
      'All components can be upgraded simultaneously in a maintenance window to minimize downtime',
    ],
    correctIndex: 1,
    answer: 'Order: (1) Review the K8s changelog and deprecation guide. (2) Upgrade the control plane via `aws eks update-cluster-version` — non-disruptive, AWS runs it in-place. (3) Update EKS managed add-ons to versions compatible with the new K8s version. (4) Upgrade node groups — managed node groups drain and replace nodes gracefully. What can go wrong: deprecated APIs in manifests break (e.g., `extensions/v1beta1` removed), add-on version incompatibilities, and PDBs blocking node drains.',
    hint: 'Control plane must be on the new version before nodes can be.',
    difficulty: 'hard',
    tags: ['upgrades', 'version', 'lifecycle'],
  },
  {
    id: 'eks-010',
    category: 'eks-aws',
    question: 'An SRE in Account B needs `kubectl` access to an EKS cluster in Account A. What is the correct setup?',
    options: [
      'Share the cluster kubeconfig file directly with the Account B engineer',
      'IAM role in Account A with trust policy allowing Account B role to assume it + map the role to a K8s RBAC group via aws-auth or Access Entry',
      'Create a VPN tunnel between Account A and B to allow direct API server access',
      'Create duplicate IAM users in Account A for each Account B engineer needing access',
    ],
    correctIndex: 1,
    answer: 'Create an IAM role in Account A with a trust policy allowing the SRE\'s role in Account B to assume it. Map this IAM role to a Kubernetes RBAC group in aws-auth or an EKS Access Entry. The SRE assumes the cross-account role via `aws sts assume-role`, then runs `aws eks update-kubeconfig --cluster-name <name> --role-arn <role-in-account-a>`. For Access Entries, you can directly associate the Account B role ARN with an access policy, which is simpler.',
    hint: 'Combine IAM cross-account role assumption with Kubernetes RBAC mapping.',
    difficulty: 'hard',
    tags: ['cross-account', 'iam', 'rbac'],
  },

  // =============================================
  // CI/CD & GITOPS (10 questions)
  // =============================================
  {
    id: 'cicd-001',
    category: 'cicd-gitops',
    question: 'ArgoCD deploys a Namespace, then a ConfigMap, then a Deployment — but without ordering, the Deployment starts before the ConfigMap exists. What solves this?',
    options: [
      'ArgoCD automatically detects resource dependencies and applies them in the correct order',
      'Sync waves: lower wave numbers applied first; ArgoCD waits for each wave to be healthy before proceeding',
      'Sync phases (PreSync/Sync/PostSync) handle ordering; waves are for multi-cluster scenarios',
      'Set `syncOrder` annotation to a timestamp on each resource to control order',
    ],
    correctIndex: 1,
    answer: 'Sync waves use `argocd.argoproj.io/sync-wave: "1"` annotations to control order. Lower wave numbers are applied first, and ArgoCD waits for resources in each wave to be healthy before the next. Example: wave 0 for Namespace and RBAC, wave 1 for ConfigMaps/Secrets, wave 2 for database StatefulSet, wave 3 for app Deployments. Without sync waves, ArgoCD applies all resources simultaneously, causing race conditions.',
    hint: 'Think about resources that must exist before other resources can start successfully.',
    difficulty: 'normal',
    tags: ['argocd', 'sync-waves', 'ordering'],
  },
  {
    id: 'cicd-002',
    category: 'cicd-gitops',
    question: 'What is the main risk of the ArgoCD App of Apps pattern?',
    options: [
      'App of Apps cannot be used with Helm charts; only Kustomize is supported',
      'Cascade delete: deleting the root app with `cascade=true` deletes all child apps and their resources',
      'The root app must be in the same repository as all child apps',
      'App of Apps only supports up to 10 child Applications per root',
    ],
    correctIndex: 1,
    answer: 'App of Apps is a pattern where a root ArgoCD Application points to a Git directory containing other Application manifests — when ArgoCD syncs the root, it creates all child Applications. The major risk: cascade deletion. If the root app is misconfigured or accidentally deleted with `cascade=true`, all child apps and their resources are deleted. It also creates a complex dependency tree that can be hard to debug when sync fails.',
    hint: 'Think about what happens if the parent is deleted.',
    difficulty: 'normal',
    tags: ['argocd', 'app-of-apps', 'patterns'],
  },
  {
    id: 'cicd-003',
    category: 'cicd-gitops',
    question: 'When should you use ApplicationSets instead of App of Apps?',
    options: [
      'App of Apps scales better for large deployments; ApplicationSets are for simple scenarios',
      'ApplicationSets are the same as App of Apps but with a different name',
      'ApplicationSets when you have repetitive patterns — e.g., deploy same service to 10 clusters or one app per repo directory, without writing each Application manifest',
      'ApplicationSets only work for single-cluster setups; App of Apps handles multi-cluster',
    ],
    correctIndex: 2,
    answer: 'ApplicationSets are a templating mechanism that generates multiple Applications from a single template and a generator (list, Git directory, cluster, matrix). Use ApplicationSets when you have a repetitive pattern — deploying the same microservice to 10 clusters, or creating one Application per directory in a monorepo. App of Apps requires writing each Application manifest manually. The Git generator is particularly powerful: it discovers directories in a repo and creates an Application per directory automatically.',
    hint: 'One requires writing each Application manifest; the other generates them from a template.',
    difficulty: 'hard',
    tags: ['argocd', 'applicationsets', 'multi-cluster'],
  },
  {
    id: 'cicd-004',
    category: 'cicd-gitops',
    question: 'Compare External Secrets Operator (ESO) and Sealed Secrets. When would you choose Sealed Secrets?',
    options: [
      'Sealed Secrets is always preferred — it uses stronger encryption than ESO',
      'ESO syncs from external stores (Secrets Manager, Vault); Sealed Secrets encrypts secrets into Git. Choose Sealed Secrets for simpler setups without an external secrets manager',
      'ESO only supports HashiCorp Vault; Sealed Secrets supports AWS Secrets Manager',
      'Sealed Secrets decrypts at build time in CI; ESO decrypts at pod startup',
    ],
    correctIndex: 1,
    answer: 'Sealed Secrets encrypts secrets client-side with `kubeseal` using the cluster\'s public key — the encrypted SealedSecret can be committed to Git. ESO syncs secrets from external stores (AWS Secrets Manager, Vault, GCP SM) into K8s Secrets via an ExternalSecret CRD. Choose ESO when you already have a secrets manager and want a single source of truth. Choose Sealed Secrets for simpler setups where you want everything in Git without managing an external store. ESO advantage: rotation happens externally.',
    hint: 'One encrypts secrets into Git; the other references secrets from an external store.',
    difficulty: 'normal',
    tags: ['secrets', 'eso', 'sealed-secrets', 'gitops'],
  },
  {
    id: 'cicd-005',
    category: 'cicd-gitops',
    question: 'Someone runs `kubectl edit` on an ArgoCD-managed Deployment. What happens if `self-heal: true` is set?',
    options: [
      'ArgoCD logs the change but does not revert it — kubectl edit is always allowed',
      'ArgoCD detects drift on the next sync cycle and reverts the manual change to match Git',
      'The Application goes Degraded and requires manual sync to recover',
      'ArgoCD blocks the kubectl edit at admission time before it applies',
    ],
    correctIndex: 1,
    answer: 'In GitOps, the Git repository is the single source of truth. When someone runs `kubectl edit` on an ArgoCD-managed resource, ArgoCD detects the drift on its next sync check (default every 3 minutes). With auto-sync and `self-heal: true`, ArgoCD reverts the change to match Git. Without self-heal, ArgoCD shows the Application as "OutOfSync" but takes no action. Self-heal ensures the cluster state always matches Git, which is why manual kubectl changes in production are a bad practice in GitOps.',
    hint: 'What does ArgoCD do when it detects the cluster state diverges from Git?',
    difficulty: 'easy',
    tags: ['gitops', 'drift', 'argocd', 'self-heal'],
  },
  {
    id: 'cicd-006',
    category: 'cicd-gitops',
    question: 'How should GitHub Actions authenticate to AWS without storing long-lived IAM keys?',
    options: [
      'Store IAM access keys as GitHub repository secrets — GitHub encrypts them with AES-256',
      'Use OIDC federation: `aws-actions/configure-aws-credentials` with `role-to-assume` — tokens expire in 15 minutes, no keys stored',
      'Use environment-level secrets which are more secure than repository secrets',
      'GitHub automatically masks IAM keys including all base64-encoded and derived versions',
    ],
    correctIndex: 1,
    answer: 'GitHub Actions supports OIDC federation with AWS. Configure `aws-actions/configure-aws-credentials` with `role-to-assume` instead of access keys. The workflow receives a short-lived OIDC token that AWS STS validates and exchanges for temporary credentials scoped to the IAM role. Tokens expire in 15 minutes — a leaked token is useless. Scope the trust policy to your specific repository and branch (`sub` claim) to prevent forks from assuming the role.',
    hint: 'Which approach means no long-lived credentials exist anywhere?',
    difficulty: 'normal',
    tags: ['github-actions', 'secrets', 'security'],
  },
  {
    id: 'cicd-007',
    category: 'cicd-gitops',
    question: 'When would you choose canary over blue-green in Argo Rollouts?',
    options: [
      'Canary is preferred when you need instant rollback capability',
      'Blue-green when you need gradual traffic shift with real user validation; canary for instant atomic cut-over',
      'Canary when you want to limit blast radius by testing with a small percentage of real traffic before full rollout',
      'Blue-green and canary provide identical blast radius control; choice is purely operational preference',
    ],
    correctIndex: 2,
    answer: 'Blue-green maintains two full environments — deploy to green, test it, then switch traffic instantly by updating the Service selector. Rollback is instant (switch back). Canary gradually shifts traffic (5%, 25%, 50%, 100%) with analysis gates between steps. Choose canary when you want to limit blast radius by testing with a small percentage of real traffic first. Choose blue-green when you need instant rollback and can afford running double the infrastructure.',
    hint: 'One shifts traffic gradually; the other swaps all traffic at once.',
    difficulty: 'normal',
    tags: ['argo-rollouts', 'canary', 'blue-green', 'deployment'],
  },
  {
    id: 'cicd-008',
    category: 'cicd-gitops',
    question: 'What is the difference between ArgoCD "OutOfSync" and "Degraded" health statuses?',
    options: [
      'OutOfSync means the app is crashing; Degraded means it is behind the latest Git commit',
      'OutOfSync = live state differs from Git; Degraded = matches Git but unhealthy at runtime',
      'A Synced application is always healthy — Degraded only applies to OutOfSync apps',
      'Degraded automatically triggers a re-sync with Git to fix the health issue',
    ],
    correctIndex: 1,
    answer: 'ArgoCD periodically compares live state against the desired state in Git. "OutOfSync" means the live state differs from Git — someone edited a resource, a webhook mutated it, or a new commit has not been synced yet. "Degraded" is a health status meaning the resource matches Git, but is unhealthy at runtime (Deployment with unavailable replicas, pod in CrashLoopBackOff). You can be "Synced + Degraded" (matches Git but broken) or "OutOfSync + Healthy" (drifted but still working). Both require attention but for different reasons.',
    hint: 'Sync status is about Git match; health status is about runtime condition.',
    difficulty: 'normal',
    tags: ['argocd', 'drift', 'health', 'sync'],
  },
  {
    id: 'cicd-009',
    category: 'cicd-gitops',
    question: 'ArgoCD hub-spoke vs. decentralized multi-cluster model — what is the main tradeoff?',
    options: [
      'Hub-spoke requires VPN between all clusters; decentralized works over the public internet',
      'Hub-spoke: one ArgoCD manages all clusters — simpler but single point of failure; decentralized: resilient but harder to maintain consistency',
      'Decentralized model only supports ApplicationSets; hub-spoke supports all Application types',
      'Hub-spoke is recommended only for fewer than 5 clusters; decentralized scales better',
    ],
    correctIndex: 1,
    answer: 'ArgoCD supports multi-cluster by registering external clusters via `argocd cluster add`. Hub-spoke runs one ArgoCD on a management cluster deploying to multiple target clusters — simpler to manage, one place for RBAC and ApplicationSets, but creates a single point of failure. Decentralized runs separate ArgoCD instances per cluster — more resilient and isolated, but harder to maintain consistency and requires a meta ArgoCD or separate tooling for coordination. Network connectivity between the management cluster and targets is a prerequisite.',
    hint: 'Centralized control vs. distributed resilience.',
    difficulty: 'expert',
    tags: ['argocd', 'multi-cluster', 'architecture'],
  },
  {
    id: 'cicd-010',
    category: 'cicd-gitops',
    question: 'Push-based CD (CI pipeline runs kubectl apply) vs. pull-based CD (ArgoCD/Flux) — what is the key security difference?',
    options: [
      'Push-based is more secure because cluster credentials are encrypted in the CI secrets store',
      'Pull-based: the agent in the cluster pulls from Git — no external system needs cluster credentials; push-based requires CI to hold cluster access',
      'They have the same security model; the difference is purely about deployment speed',
      'Push-based CD requires read-only cluster access; pull-based needs write access',
    ],
    correctIndex: 1,
    answer: 'Push-based CD requires the CI system to have cluster credentials (kubeconfig, service account token), creating a wide attack surface — if CI is compromised, the attacker has direct cluster access. Pull-based (GitOps) inverts this: the agent inside the cluster pulls changes from Git, so no external system needs cluster credentials. Git becomes the only interface, controlled via Git permissions. Trade-off: push-based is simpler and provides immediate CI feedback; pull-based requires running a controller in-cluster and has an eventual consistency model.',
    hint: 'Where do cluster credentials live in each model?',
    difficulty: 'normal',
    tags: ['gitops', 'ci-cd', 'security', 'push-vs-pull'],
  },

  // =============================================
  // ARCHITECTURE (10 questions)
  // =============================================
  {
    id: 'arch-001',
    category: 'architecture',
    question: 'Your product launch is in 2 weeks and you expect 10x normal traffic. Walk through your scaling strategy — what layers do you address and in what order?',
    options: [
      'Scale up the database instance to handle the traffic surge first, then scale app servers reactively via HPA',
      'CDN for static assets, Redis caching for hot reads, read replicas for DB, pre-warm the load balancer and ASG/Karpenter ahead of launch — scale out not up, proactively not reactively',
      'Double all EC2 instance sizes the night before and scale back after the event to minimize risk',
      'Set HPA maxReplicas to 10x and let auto-scaling handle everything dynamically during the event',
    ],
    correctIndex: 1,
    answer: 'Work from the outside in. CDN absorbs static traffic and reduces origin load (often 60-80% of requests never hit your servers). Cache hot reads in Redis to protect the database. Add read replicas so read-heavy queries do not hit the primary. Pre-warm LB and set min replicas ahead of time — reactive auto-scaling has a lag and the first spike will hit before new nodes are ready. Vertical scaling (bigger instances) does not scale linearly and creates a single point of failure. Load test against the target volume before launch, not after.',
    hint: 'Reactive auto-scaling has lag. What absorbs the first spike before new nodes boot?',
    difficulty: 'normal',
    tags: ['scaling', 'performance', 'high-availability', 'caching'],
  },
  {
    id: 'arch-002',
    category: 'architecture',
    question: 'Service A calls Service B synchronously. Service B is slow (10s response time). You have 5 services calling each other in chains. What failure mode does this create and what fixes it?',
    options: [
      'Retry with exponential backoff on the caller side prevents cascades — failed calls retry until B recovers',
      'Circuit breaker on the caller: open after N consecutive failures, fail fast instead of holding threads on timeout; prevents downstream slowness from cascading upstream and exhausting thread pools',
      'Set a 1-second timeout on all service calls — timeouts alone are sufficient to prevent cascades',
      'Add a load balancer in front of B to distribute the slow requests across multiple instances',
    ],
    correctIndex: 1,
    answer: 'When B is slow, A\'s threads pile up waiting. A becomes slow, and now everything calling A piles up too — a cascade failure. The circuit breaker pattern opens after N failures (e.g., 5 in 10s), making A return an error immediately instead of waiting. This prevents thread pool exhaustion upstream. After a "half-open" probe period, it closes if B recovers. Retry with backoff helps for transient errors but amplifies load during an outage and does nothing to prevent thread exhaustion. Always combine: timeouts + circuit breakers + bulkheads (separate thread pools per dependency).',
    hint: 'Threads waiting on slow B pile up. What happens to services calling A?',
    difficulty: 'normal',
    tags: ['circuit-breaker', 'resilience', 'distributed-systems', 'cascade-failure'],
  },
  {
    id: 'arch-003',
    category: 'architecture',
    question: 'You need zero-downtime deployment for a service with a DB migration that adds a non-nullable column. What pattern do you use?',
    options: [
      'Deploy new code and run the migration simultaneously in the same pipeline step — the migration runs before new pods start',
      'Use a feature flag to hide the new field from old pods, then flip the flag after full rollout',
      'Expand-contract: add column as nullable → deploy code that writes to it → backfill → add NOT NULL constraint in a separate later deployment',
      'Run the migration as a Kubernetes init container so it completes before the first new pod starts',
    ],
    correctIndex: 2,
    answer: 'Use expand-contract (parallel change). Phase 1 (expand): add the column as nullable, deploy new code that writes to it but does not require it. Old pods still work — they do not know the column exists. Phase 2: backfill existing rows. Phase 3 (contract): add NOT NULL constraint, deploy code that requires the column. Each phase is independently rollback-safe. The init container approach fails because old pods running during the rolling update will break on INSERT — they do not supply the new column. Never deploy a breaking schema change and new code in the same deployment.',
    hint: 'During a rolling update, old and new code run simultaneously. Both must be able to write to the DB.',
    difficulty: 'expert',
    tags: ['zero-downtime', 'database', 'migrations', 'deployment'],
  },
  {
    id: 'arch-004',
    category: 'architecture',
    question: 'Your PostgreSQL primary is at 90% CPU under 50K queries/sec. What is the right scaling progression?',
    options: [
      'Vertically scale the primary instance immediately — more CPU is the fastest fix',
      'Immediately shard across 10 databases to distribute the write load evenly',
      'Add read replicas for read-heavy traffic (typically 80% of queries), tune query performance, then vertically scale writes; shard only when writes become the bottleneck',
      'Migrate to DynamoDB — relational databases cannot handle this query volume',
    ],
    correctIndex: 2,
    answer: 'Most database traffic is reads (typically 70-90%). Add read replicas and route SELECT queries there via a proxy (PgBouncer, RDS Proxy). This often eliminates the primary CPU spike with no schema changes. Next, optimize slow queries with EXPLAIN ANALYZE — often 1-2 bad queries cause most CPU. Then right-size the primary (vertical scale). Sharding (horizontal partitioning) is expensive to implement and operate — it splits your data across databases, complicates joins and transactions, and should only be reached when single-node writes are genuinely maxed out. NoSQL is a different data model, not a performance shortcut.',
    hint: 'Reads and writes scale differently. What does most of that 50K queries/sec actually consist of?',
    difficulty: 'hard',
    tags: ['database', 'scaling', 'read-replicas', 'sharding'],
  },
  {
    id: 'arch-005',
    category: 'architecture',
    question: 'A payment service must charge a card (external API) AND record the transaction in your database. Both must succeed or both must fail. You cannot wrap an HTTP call in a database transaction. What pattern handles this?',
    options: [
      'Write to DB first, then call the payment API. If the API fails, roll back the DB row',
      'Use XA (two-phase commit) distributed transactions between your DB and the payment provider',
      'Saga pattern: each step is a local transaction with a compensating action on failure. If payment succeeds but DB write fails, issue a refund as the compensating action',
      'Use an outbox table: write the charge intent to DB, then a background job calls the payment API',
    ],
    correctIndex: 2,
    answer: 'XA/2PC requires the external system to support it — payment providers do not. Writing DB first and rolling back on API failure has a gap: the DB write can succeed, API call fails, and your rollback also fails (network partition). The Saga pattern splits the distributed operation into local transactions. Each step has a compensating action: if payment succeeds but DB write fails, trigger a refund. The outbox pattern (option D) is also valid — it guarantees the payment call happens at-least-once via a transactional outbox, but needs idempotency at the payment API. For payments specifically, idempotency keys are essential regardless of pattern.',
    hint: 'You cannot wrap an HTTP call in a DB transaction. How do you achieve "either both or neither" across two independent systems?',
    difficulty: 'expert',
    tags: ['distributed-systems', 'saga', 'consistency', 'transactions'],
  },
  {
    id: 'arch-006',
    category: 'architecture',
    question: 'Your business needs 99.99% uptime (52 minutes downtime/year). Your current stack runs in a single AWS region with multi-AZ. Is this sufficient?',
    options: [
      'Yes — multi-AZ in a single region provides 99.99% availability because AZ failures are independent',
      'No — single region has region-level failure risk. 99.99% requires multi-region active-passive minimum, with Route53 health check failover and cross-region DB replication',
      'Yes — add a CDN in front for static content and the dynamic origin uptime target becomes easier to hit',
      'It depends on the RDS Multi-AZ configuration — that single component determines the overall uptime SLA',
    ],
    correctIndex: 1,
    answer: 'AWS single-region multi-AZ can realistically achieve 99.9-99.95%. For 99.99% (52 min/year), you need to account for regional control plane failures, regional API outages, and regional networking issues that affect all AZs simultaneously. 99.99% requires multi-region: a warm standby in a second region with cross-region DB replication (RDS read replica promoted on failover, or Aurora Global Database with < 1s replication lag), Route53 health checks with sub-minute failover, and regular failover drills. Calculate your actual availability: it is the product of all dependencies. One dependency at 99.9% limits your overall SLA to 99.9%.',
    hint: 'Multi-AZ protects against AZ failure. What does it not protect against?',
    difficulty: 'hard',
    tags: ['high-availability', 'multi-region', 'disaster-recovery', 'sla'],
  },
  {
    id: 'arch-007',
    category: 'architecture',
    question: 'You are migrating a Rails monolith to microservices. The monolith has 200K lines of code. What is the right approach?',
    options: [
      'Stop all feature development, rewrite the monolith in Go microservices over 6 months, then cut over',
      'Split the monolith by technical layers first: separate the frontend, API, and database tiers',
      'Strangler fig: route new traffic via an API gateway, extract high-value bounded contexts as services one at a time, keep the monolith running until it is fully replaced',
      'Extract all services simultaneously in parallel streams to complete the migration faster',
    ],
    correctIndex: 2,
    answer: 'The strangler fig pattern (Martin Fowler) is the safest migration path. Add an API gateway in front of the monolith. Extract one bounded context at a time — start with the highest-value or least-coupled service. The gateway routes that traffic to the new service; everything else still hits the monolith. The monolith shrinks incrementally. Full rewrites (the "big bang") have a very high failure rate — 6+ months of no new features, the rewritten system diverges from production behavior, and you cut over all at once. Splitting by technical layers creates distributed monolith — all services are still tightly coupled, just via network instead of function calls.',
    hint: 'What approach lets you migrate incrementally while keeping the business running?',
    difficulty: 'normal',
    tags: ['migration', 'microservices', 'strangler-fig', 'architecture'],
  },
  {
    id: 'arch-008',
    category: 'architecture',
    question: 'Service B processes events from a queue. Queue depth grows to 1M messages because B is slow. How do you design for this operationally?',
    options: [
      'Increase B\'s instance size until it can keep up — vertical scaling is simpler than complex queue management',
      'Set a message TTL so old messages expire automatically and the queue self-regulates',
      'Dead letter queue for poison messages (prevents infinite retries blocking the queue), autoscale B on queue depth via KEDA, alert on DLQ messages and queue age (not just depth)',
      'Switch to synchronous calls so B\'s slowness applies natural backpressure to producers',
    ],
    correctIndex: 2,
    answer: 'The first problem is poison messages — one malformed event that B cannot process will block the queue forever if it retries infinitely. DLQ catches messages that exceed maxReceiveCount. Alert on DLQ messages immediately since they represent lost work. Scale B horizontally based on queue depth (KEDA SQS scaler scales consumer pods to match queue depth). Alert on message age (ApproximateAgeOfOldestMessage) not just depth — age tells you how far behind real-time you are. Vertical scaling hits a ceiling. Synchronous backpressure is useful but kills producer throughput and cannot handle burst load.',
    hint: 'A single bad message can block the whole queue. What prevents that, and how do you know when to scale B?',
    difficulty: 'hard',
    tags: ['messaging', 'queue', 'dead-letter-queue', 'keda', 'resilience'],
  },
  {
    id: 'arch-009',
    category: 'architecture',
    question: 'Your company handles PCI-compliant payment data. You are designing the Kubernetes networking for the payment service. What is the minimum required isolation?',
    options: [
      'Run the payment service in a separate namespace with RBAC restrictions on who can exec into pods',
      'Use network policies to restrict ingress/egress — namespaces do not provide network isolation by default',
      'Dedicated namespace with default-deny NetworkPolicies (only allow specific ports from specific namespaces), mTLS between services, no shared node affinity with non-PCI workloads, and separate secrets scope',
      'A separate cluster for PCI workloads is always required — namespaces cannot provide sufficient compliance boundary',
    ],
    correctIndex: 2,
    answer: 'PCI DSS requires a Cardholder Data Environment (CDE) with strict network segmentation. In Kubernetes: dedicated namespace, default-deny NetworkPolicy (whitelist only required traffic), mTLS between services (service mesh or cert-manager), node affinity rules to prevent scheduling on shared nodes (use taints/tolerations), audit logging on every API call to the namespace, and image scanning + admission control. A separate cluster provides stronger isolation and is often chosen for PCI CDE, but it is not always required — a well-configured namespace-based CDE with the above controls can satisfy the segmentation requirement. Document your control rationale for auditors.',
    hint: 'Namespace names alone provide zero network isolation in Kubernetes.',
    difficulty: 'expert',
    tags: ['security', 'pci', 'network-policy', 'isolation', 'compliance'],
  },
  {
    id: 'arch-010',
    category: 'architecture',
    question: 'Your platform serves 100 product teams. Each team deploys independently. The release pipeline takes 45 minutes end-to-end and is becoming a bottleneck. What architectural interventions have the highest impact?',
    options: [
      'Buy faster CI runners — reducing build time by 20% scales linearly with team count',
      'Reduce test coverage to the critical path only so pipelines run faster',
      'Parallelize pipeline stages (build/test/scan concurrently), per-service build caches keyed by dependency hash, move integration tests to async post-merge, decouple deploy from CI (push to Git → ArgoCD pulls) so pipeline does not wait for Kubernetes rollout',
      'Consolidate all 100 teams onto a shared monorepo pipeline — one pipeline for all is more efficient than 100 separate ones',
    ],
    correctIndex: 2,
    answer: 'Sequential pipeline stages are the biggest time sink — build, test, and security scan can run concurrently with proper dependency gates. Per-service build caches (Docker layer cache, npm cache keyed by lockfile hash) avoid re-running work when inputs have not changed. Async integration tests unblock the deploy — run them post-merge and gate the next release on results rather than the current one. Decoupling deploy from CI (GitOps pull model) means the pipeline does not poll for rollout completion, cutting minutes per deploy. At 100 teams, 5 minutes saved per pipeline run = 500 engineer-minutes/day. Measure pipeline stage durations first — optimize the longest stage, not the easiest one.',
    hint: 'Most pipeline time is sequential waiting. What can run in parallel, and what can be moved out of the critical path?',
    difficulty: 'hard',
    tags: ['platform-engineering', 'ci-cd', 'developer-experience', 'scaling'],
  },

  // =============================================
  // OBSERVABILITY (5 questions)
  // =============================================
  {
    id: 'obs-001',
    category: 'observability',
    question: 'Your API server has high latency. Which monitoring methodology do you apply and which metrics do you look at?',
    options: [
      'USE method — check Utilization, Saturation, and Errors on the API server',
      'RED method — check Rate (requests/sec), Errors (error rate), and Duration (latency percentiles)',
      'RED for the database; USE for the API server',
      'RED and USE are interchangeable — apply whichever your team is familiar with',
    ],
    correctIndex: 1,
    answer: 'RED (Rate, Errors, Duration) is for request-driven services — it tells you how your users experience the service. Apply RED to API servers, web services, and microservice endpoints. USE (Utilization, Saturation, Errors) is for infrastructure resources (nodes, databases, network links). In practice you need both: RED for the application layer and USE for the infrastructure layer underneath. For API latency: RED metrics (p99 latency, error rate) tell you the symptom; USE metrics (CPU saturation, memory utilization) help find the cause.',
    hint: 'One is for services (user-facing); the other is for resources (infrastructure).',
    difficulty: 'normal',
    tags: ['red', 'use', 'monitoring', 'methodology'],
  },
  {
    id: 'obs-002',
    category: 'observability',
    question: 'Your Grafana dashboard is slow because it runs heavy PromQL aggregations over 1000 pods on every load. What is the fix?',
    options: [
      'Increase Prometheus query timeout — slow queries just need more time',
      'Use recording rules to pre-compute the expensive aggregation on a schedule and store it as a new time series',
      'Switch to Thanos which handles large-scale queries more efficiently than Prometheus',
      'Reduce the dashboard refresh interval to decrease query frequency',
    ],
    correctIndex: 1,
    answer: 'Recording rules pre-compute expensive PromQL queries on a schedule and store the result as a new time series. Without recording rules, every dashboard load re-evaluates the query against raw data. With a recording rule, Prometheus computes it every 30 seconds and stores the result, making dashboard queries instant. Recording rules also enable you to create aggregated SLI metrics that alerting rules can reference efficiently. Alerting rules evaluate PromQL and fire alerts — they do not pre-compute or store results.',
    hint: 'Trading storage for query speed.',
    difficulty: 'normal',
    tags: ['prometheus', 'recording-rules', 'alerting', 'performance'],
  },
  {
    id: 'obs-003',
    category: 'observability',
    question: 'Your Prometheus server keeps OOMKilling. The TSDB status page shows 50 billion active time series. What is the root cause?',
    options: [
      'Too many scrape targets — reduce the number of services being monitored',
      'High cardinality: too many unique label value combinations (e.g., a `user_id` label) creating billions of time series',
      'Prometheus retention period is too long — reduce it to 7 days',
      'Recording rules are creating duplicate time series and exhausting memory',
    ],
    correctIndex: 1,
    answer: 'Cardinality is the number of unique time series, determined by every unique combination of metric name and label values. A metric with a `user_id` label with 100K users creates 100K series per metric. Prometheus stores each series in memory — high cardinality causes OOM. Common causes: user IDs, request IDs, IP addresses, any unbounded label value. Fix: remove high-cardinality labels, use `relabel_configs` to drop or hash label values before ingestion, and use recording rules to aggregate.',
    hint: 'Every unique label combination creates a new time series in memory.',
    difficulty: 'expert',
    tags: ['prometheus', 'cardinality', 'performance', 'troubleshooting'],
  },
  {
    id: 'obs-004',
    category: 'observability',
    question: 'You want to capture all traces for errors and slow requests, but only 1% of normal traffic. What sampling strategy is this?',
    options: [
      'Head-based sampling at 1% — fast and simple to configure',
      'Tail-based sampling: collect all spans first, then decide to keep based on outcome (error or latency)',
      'Probabilistic sampling with separate error and latency samplers',
      'Rate-limiting sampling: allow at most 1% of spans per second regardless of outcome',
    ],
    correctIndex: 1,
    answer: 'Tail-based sampling collects all spans, then decides after the trace completes whether to keep it — keeping all error traces and high-latency traces while sampling down normal traffic. Head-based sampling decides at trace start (simple but misses rare errors because the decision is made before the outcome is known). Tail-based requires a collector with buffer memory (OpenTelemetry Collector with `tail_sampling` processor). Practical approach: 100% sampling for errors and p99 latency violations, 1-5% for normal traffic.',
    hint: 'Does the sampling decision happen at the beginning or end of the trace?',
    difficulty: 'hard',
    tags: ['tracing', 'sampling', 'opentelemetry'],
  },
  {
    id: 'obs-005',
    category: 'observability',
    question: 'Your SLO is 99.9%. You breach this in week 3 of the month. What does this mean for your remaining error budget?',
    options: [
      'The error budget resets weekly — week 4 starts fresh regardless of week 3 performance',
      'The error budget is exhausted — teams should freeze risky deployments and prioritize reliability work for the rest of the month',
      'SLO breach automatically triggers an SLA penalty for customers',
      'The SLO should be reduced to 99.5% to account for realistic achievable performance',
    ],
    correctIndex: 1,
    answer: 'SLI is the measurement (e.g., % of requests < 300ms). SLO is the internal target (e.g., 99.9% over 30-day rolling window). The error budget is 100% - SLO = 0.1% for 99.9% SLO (about 43 minutes/month of allowed downtime). When the budget is exhausted, the policy is to freeze risky deployments (new features, experiments) and focus engineering effort on reliability improvements. SLO is internal; SLA is the external contract with customers with consequences (service credits). SLO should always be stricter than SLA to give a buffer.',
    hint: 'Error budget = 100% − SLO. What happens when it runs out?',
    difficulty: 'easy',
    tags: ['sli', 'slo', 'sla', 'sre'],
  },

  // =============================================
  // TERRAFORM (additional questions)
  // =============================================
  {
    id: 'tf-011',
    category: 'terraform',
    question: 'What is the purpose of the `terraform providers lock` command?',
    options: [
      'It prevents other team members from running terraform apply simultaneously',
      'It records the cryptographic hashes of provider plugins in .terraform.lock.hcl to ensure consistent installs across machines and CI',
      'It locks a specific resource so it cannot be modified by any configuration change',
      'It pins the Terraform CLI version required by the configuration',
    ],
    correctIndex: 1,
    answer: 'The `terraform providers lock` command updates the dependency lock file (.terraform.lock.hcl) with the cryptographic hashes of provider packages, ensuring all team members and CI pipelines install the exact same provider binaries. It does not prevent concurrent runs — that is state locking\'s job. Resource-level protection is done with `lifecycle { prevent_destroy }`, and CLI version pinning is done via the `required_version` constraint.',
    hint: 'Think about supply-chain security and reproducible provider installations across different machines.',
    difficulty: 'normal',
    tags: ['providers', 'lock-file', 'reproducibility', 'security'],
  },
  {
    id: 'tf-012',
    category: 'terraform',
    question: 'You need to remove a resource from Terraform management without destroying it. Which approach is correct?',
    options: [
      'Delete the resource block from the configuration and run `terraform apply`',
      'Run `terraform state rm <resource_address>` to remove it from state, then delete the config block',
      'Add `lifecycle { ignore_changes = all }` to the resource block',
      'Run `terraform destroy -target=<resource_address>` followed by deleting the config block',
    ],
    correctIndex: 1,
    answer: '`terraform state rm` removes the resource from the state file without touching the real infrastructure, effectively orphaning it from Terraform management. Deleting the config and running apply without first removing from state would cause Terraform to destroy the real resource. `ignore_changes = all` keeps the resource in state but ignores drift — it does not remove Terraform management. `terraform destroy -target` would actually delete the real infrastructure.',
    hint: 'There are two steps: decouple the state first, then remove the configuration.',
    difficulty: 'normal',
    tags: ['state', 'lifecycle', 'migration', 'state-rm'],
  },
  {
    id: 'tf-013',
    category: 'terraform',
    question: 'Which Terraform feature allows you to write tests that validate module outputs without deploying real infrastructure?',
    options: [
      '`terraform validate` — checks syntax and type correctness',
      '`precondition` blocks inside resource lifecycle — validate at apply time',
      'The `terraform test` command with `.tftest.hcl` files and `mock_provider` blocks — unit tests without real API calls',
      'Sentinel policies in Terraform Cloud — governance checks on plan output',
    ],
    correctIndex: 2,
    answer: 'Terraform 1.6+ introduced the `terraform test` command with `.tftest.hcl` test files that support `mock_provider` blocks, enabling unit-style tests that validate module logic without making real API calls. `terraform validate` only checks syntax and type correctness. Preconditions validate at apply time against real infrastructure. Sentinel is a policy-as-code governance framework, not a unit testing tool.',
    hint: 'This was introduced in Terraform 1.6 and involves a specific new file extension.',
    difficulty: 'hard',
    tags: ['testing', 'mock-provider', 'tftest', 'modules'],
  },
  {
    id: 'tf-014',
    category: 'terraform',
    question: 'What is the key risk of using `terraform apply -auto-approve` in a CI/CD pipeline targeting production?',
    options: [
      'It skips provider version checks, potentially using an incompatible provider binary',
      'It bypasses the plan review step — any unintended destructive change (resource replacement, deletion) is applied without a human gate',
      'It disables state locking, creating a race condition if two pipelines run simultaneously',
      'It forces a full resource refresh, causing unnecessary API calls and potential rate limiting',
    ],
    correctIndex: 1,
    answer: 'The `-auto-approve` flag skips the interactive confirmation that normally follows `terraform plan`, meaning any destructive diff — resource replacements, deletions — proceeds immediately without a human reviewing the plan. State locking is independent of `-auto-approve`. Provider version checks still run. A full refresh is not forced by this flag. Safe pattern: run plan in CI, require manual approval of the plan artifact, then apply with `-auto-approve` only after explicit approval.',
    hint: 'Consider what the normal interactive workflow gives you that this flag removes.',
    difficulty: 'easy',
    tags: ['ci-cd', 'safety', 'auto-approve', 'production'],
  },
  {
    id: 'tf-015',
    category: 'terraform',
    question: 'A shared networking module used by 15 root modules has an output removed in v2.3.0. All consumers pin `version = "~> 2.1"`. What is the safest rollout?',
    options: [
      'Update the module version and run `terraform apply` across all 15 consumers simultaneously in CI',
      'Audit all consumers, update each to stop referencing the removed output, then release the new module version — never remove an output while consumers still reference it',
      'Add `ignore_changes` in each consumer to suppress the broken reference errors',
      'Use `terraform state mv` to migrate the output references to a new state path',
    ],
    correctIndex: 1,
    answer: 'Removing a module output that consumers still reference breaks all dependent configurations at plan time. The correct approach is expand-contract applied to module interfaces: first update all consumers to stop referencing the deprecated output, then release the new module version. Simultaneous apply breaks in-flight pipelines. `ignore_changes` applies to resource attributes, not module output references. `state mv` does not fix broken HCL references.',
    hint: 'Same as the expand-contract DB migration pattern — never remove a contract while consumers still depend on it.',
    difficulty: 'expert',
    tags: ['modules', 'versioning', 'monorepo', 'breaking-changes'],
  },

  // =============================================
  // KUBERNETES (additional questions)
  // =============================================
  {
    id: 'k8s-016',
    category: 'kubernetes',
    question: 'What does `terminationGracePeriodSeconds` control, and what happens if a container does not exit within that window?',
    options: [
      'It sets how long the kubelet waits before starting a new pod during a rolling update',
      'It defines how long Kubernetes waits after sending SIGTERM before sending SIGKILL to force-terminate the container',
      'It controls how long a container can stay in Terminating state before the node is cordoned',
      'It sets the timeout for readiness probe failures before the pod is removed from service endpoints',
    ],
    correctIndex: 1,
    answer: 'When a pod is deleted, Kubernetes sends SIGTERM to the container and waits `terminationGracePeriodSeconds` (default 30s) for a graceful shutdown. If the process has not exited by then, SIGKILL is sent — which cannot be caught or ignored. This matters for Spot instance interruption handling, node drains, and rolling updates. It is unrelated to rolling update pacing, node cordoning, or readiness probe behavior.',
    hint: 'Two-signal shutdown sequence: one catchable, one not.',
    difficulty: 'normal',
    tags: ['lifecycle', 'graceful-shutdown', 'sigterm', 'sigkill'],
  },
  {
    id: 'k8s-017',
    category: 'kubernetes',
    question: 'A Deployment has `maxUnavailable: 0` and `maxSurge: 1`. What is the behavioral consequence during a rollout?',
    options: [
      'All old pods are terminated first, then new pods start — equivalent to Recreate strategy',
      'One new pod is created and must become Ready before an old pod is terminated — full capacity maintained throughout',
      'The rollout pauses after the first new pod and waits for manual promotion',
      'New pods are created only on surge nodes, preventing scheduling on existing nodes',
    ],
    correctIndex: 1,
    answer: 'With `maxUnavailable: 0`, no existing pod can be removed until a replacement is Ready — the desired replica count is always available. `maxSurge: 1` allows one extra pod above desired count at a time. Together: create new → wait for Ready → delete old, one pod at a time. This is the safest rolling update profile for production traffic but the slowest rollout speed.',
    hint: 'Combine both constraints: what must be true before any old pod can be removed?',
    difficulty: 'normal',
    tags: ['rolling-update', 'deployment-strategy', 'availability', 'maxsurge'],
  },
  {
    id: 'k8s-018',
    category: 'kubernetes',
    question: 'What happens if a Kubernetes controller that owns a finalizer is deleted while objects with that finalizer still exist?',
    options: [
      'Finalizers trigger cleanup Jobs automatically; Jobs run without the controller',
      'Objects remain stuck in Terminating state indefinitely — no controller exists to remove the finalizer',
      'Deletion completes after the default 30-second grace period regardless of finalizer state',
      'The API server removes orphaned finalizers automatically after 10 minutes',
    ],
    correctIndex: 1,
    answer: 'A finalizer is an opaque string in `metadata.finalizers` that tells the API server: do not delete this object until all finalizers are removed. A controller watches for objects with its finalizer, performs cleanup (e.g., deletes cloud resources), then removes the finalizer to unblock deletion. If the controller is uninstalled while finalized objects exist, those objects remain Terminating forever. This is a common operational hazard when uninstalling operators like cert-manager or Crossplane without proper cleanup order.',
    hint: 'Who is responsible for removing the finalizer — and what if they are gone?',
    difficulty: 'hard',
    tags: ['finalizers', 'operators', 'terminating', 'deletion'],
  },
  {
    id: 'k8s-019',
    category: 'kubernetes',
    question: 'When would you use a ValidatingAdmissionWebhook over a MutatingAdmissionWebhook?',
    options: [
      'ValidatingAdmissionWebhook runs before MutatingAdmissionWebhook and can modify objects to set defaults',
      'ValidatingAdmissionWebhook cannot modify objects — it can only allow or reject; use it for policy enforcement like requiring labels or blocking privileged containers',
      'ValidatingAdmissionWebhook is for CRD validation only; MutatingAdmissionWebhook handles core Kubernetes resources',
      'They are interchangeable — the distinction is naming convention only',
    ],
    correctIndex: 1,
    answer: 'MutatingAdmissionWebhooks can alter the incoming object (inject sidecars, set defaults, add annotations). ValidatingAdmissionWebhooks can only inspect and accept/reject — no modifications. Mutating webhooks run first, then validating webhooks run on the potentially modified final object. Use ValidatingAdmissionWebhooks for pure policy enforcement (OPA/Gatekeeper, Kyverno deny rules) where idempotency and auditability matter.',
    hint: 'One changes the request; the other only judges it.',
    difficulty: 'easy',
    tags: ['admission-control', 'webhooks', 'policy', 'security'],
  },
  {
    id: 'k8s-020',
    category: 'kubernetes',
    question: 'Team A\'s batch jobs cause noisy-neighbor CPU contention degrading Team B\'s latency-sensitive API on shared nodes. Without separate node pools, what Kubernetes mechanisms most directly mitigate this?',
    options: [
      'Set ResourceQuotas on Team A\'s namespace to cap total CPU requests',
      'CPU limits on Team A\'s pods (CFS throttling) combined with higher PriorityClass on Team B\'s pods (preemption under pressure)',
      'Apply a NetworkPolicy to restrict Team A\'s pod network bandwidth',
      'LimitRange with default CPU requests prevents co-location of Team A and Team B pods on the same node',
    ],
    correctIndex: 1,
    answer: 'CPU limits cause the Linux CFS scheduler to throttle pods that exceed their quota, directly reducing noisy-neighbor CPU stealing. PriorityClasses ensure that under node pressure, lower-priority batch workloads are preempted first to free resources for latency-sensitive services. ResourceQuotas cap total namespace consumption but do not prevent bursting on a single node. NetworkPolicy controls L4 traffic, not CPU. LimitRanges set default requests/limits but do not influence scheduling topology.',
    hint: 'Two mechanisms needed: one for steady-state throttling, one for preemption under node pressure.',
    difficulty: 'expert',
    tags: ['multi-tenancy', 'cpu-limits', 'priorityclass', 'noisy-neighbor'],
  },

  // =============================================
  // EKS & AWS (additional questions)
  // =============================================
  {
    id: 'eks-011',
    category: 'eks-aws',
    question: 'How does EKS Pod Identity (2023) differ from IRSA?',
    options: [
      'It replaces the VPC CNI plugin for assigning IP addresses to pods on Nitro instances',
      'Pod Identity uses an agent DaemonSet on each node to broker credentials — no OIDC provider configuration required, making IAM role association simpler and more portable across clusters',
      'It is a metadata service proxy that blocks IMDS access for all pods except those with explicit allow annotations',
      'It is an admission webhook that injects AWS credentials as environment variables into pod specs at deploy time',
    ],
    correctIndex: 1,
    answer: 'EKS Pod Identity works via the `eks-pod-identity-agent` DaemonSet. Instead of requiring an OIDC identity provider setup and per-cluster trust policy configuration (IRSA), Pod Identity uses an agent that intercepts credential requests from pods and exchanges them for temporary AWS credentials. The IAM trust policy is simpler (no OIDC ARN needed) and roles are portable across clusters without updating trust policies.',
    hint: 'This 2023 feature reduces the OIDC configuration complexity of its predecessor.',
    difficulty: 'normal',
    tags: ['pod-identity', 'irsa', 'iam', 'eks'],
  },
  {
    id: 'eks-012',
    category: 'eks-aws',
    question: 'A critical CVE is published for the AMI used by your EKS managed node group. What is the correct remediation process?',
    options: [
      'Terminate all EC2 instances in the ASG — EKS will automatically launch replacements with the patched AMI',
      'Update the node group to the patched AMI version via the EKS API — it orchestrates a rolling node replacement that cordons, drains, and replaces nodes while respecting PDBs',
      'Run `kubectl drain` on each node manually then terminate the EC2 instances via the AWS console',
      'Managed node groups cannot be updated in-place — create a new node group with the patched AMI',
    ],
    correctIndex: 1,
    answer: 'Managed node groups support in-place AMI updates via `UpdateNodegroupVersion` API (console, CLI, or Terraform). EKS orchestrates the rollout: cordon node → drain workloads (respecting PDBs) → terminate old instance → launch new with patched AMI. Terminating instances directly bypasses drain and can cause disruption. Manual drain-then-terminate works but is error-prone. Managed node groups absolutely support in-place updates — that is a core feature.',
    hint: 'Managed node groups exist specifically to automate this operational task safely.',
    difficulty: 'easy',
    tags: ['managed-node-groups', 'ami', 'patching', 'security'],
  },
  {
    id: 'eks-013',
    category: 'eks-aws',
    question: 'A node running an EBS-backed pod becomes unresponsive. The replacement pod stays in Pending with a volume attachment error. Why, and how do you fix it?',
    options: [
      'The EBS volume is in the wrong AZ for the new node — resize the PV to trigger re-provisioning in the correct AZ',
      'EBS is single-attach. The old node\'s attachment is not released until the Node object is deleted from Kubernetes. Delete the unresponsive Node object to trigger force-detach and unblock the new pod.',
      'The StorageClass needs `volumeBindingMode: WaitForFirstConsumer` to allow cross-AZ scheduling',
      'Restart the EBS CSI controller pod to clear the stale attachment lock from its in-memory cache',
    ],
    correctIndex: 1,
    answer: 'EBS volumes are single-attach — one EC2 instance at a time. When a node goes unresponsive, Kubernetes does not immediately delete the Node object (node lifecycle controller waits for timeout). Until the Node object is deleted, the EBS CSI driver cannot safely force-detach, so the replacement pod stays Pending. Deleting the unresponsive Node object signals the CSI driver that the old attachment is gone. Resizing the PV is irrelevant. `WaitForFirstConsumer` helps with initial AZ alignment, not attachment locks.',
    hint: 'Two things must be true: EBS can only attach to one instance, and the Kubernetes control plane must know the old instance is gone.',
    difficulty: 'hard',
    tags: ['ebs', 'csi', 'stateful', 'node-failure'],
  },
  {
    id: 'eks-014',
    category: 'eks-aws',
    question: 'You enable both the EKS public endpoint and private endpoint for the API server. What security posture does this provide?',
    options: [
      'Public endpoint is for read operations; private endpoint is for write operations — enabling both allows read-write separation',
      'Traffic from within the VPC (kubelets, in-VPC CI runners) uses the private endpoint and stays off the internet; external access (developers, GitHub Actions) uses the public endpoint — both protected by IAM auth and RBAC',
      'Public endpoint uses certificates from AWS CA; private endpoint uses self-signed certificates — enabling both provides certificate fallback',
      'Public endpoint uses port 443; private endpoint uses port 6443 — enabling both provides redundancy if one port is blocked',
    ],
    correctIndex: 1,
    answer: 'EKS API server endpoints control the network path, not operation type. Public endpoint: reachable over the internet (still protected by AWS IAM SigV4 + RBAC). Private endpoint: VPC endpoint so in-cluster traffic routes privately without leaving AWS. Enabling both is a common production pattern — internal traffic (nodes, in-VPC CI) stays private, while external callers (developers, remote CI) can still reach the public endpoint. Restrict the public endpoint to specific CIDRs for extra hardening.',
    hint: 'Think about two different callers: a developer on the internet vs. a kubelet running inside the cluster.',
    difficulty: 'normal',
    tags: ['networking', 'api-server', 'private-endpoint', 'security'],
  },
  {
    id: 'eks-015',
    category: 'eks-aws',
    question: 'Karpenter consistently provisions new nodes in only one AZ despite all three AZs being available. What is the most likely root cause?',
    options: [
      'Karpenter\'s `consolidationPolicy: WhenUnderutilized` prefers the AZ with the most existing capacity',
      'Pods have PVCs bound to EBS volumes in that AZ — Karpenter respects topology constraints and must provision in the AZ where the volume lives',
      'The NodePool\'s `amiSelectorTerms` only match AMIs available in one AZ',
      'Karpenter defaults to the AZ with the lowest on-demand price',
    ],
    correctIndex: 1,
    answer: 'When a pod has a PVC with `volumeBindingMode: WaitForFirstConsumer`, the volume binds to a specific AZ on first scheduling. Subsequent pods using that PVC are topologically constrained to that AZ via `topology.kubernetes.io/zone`. Karpenter respects topology constraints and provisions nodes where they are needed. Consolidation policy affects existing node utilization, not AZ selection. Karpenter does not default to cheapest AZ. AMI availability issues are less common and would show as provisioning errors, not AZ skew.',
    hint: 'What external resource could be anchoring pod scheduling to a specific AZ?',
    difficulty: 'expert',
    tags: ['karpenter', 'topology', 'pvc', 'availability-zones'],
  },

  // =============================================
  // CI/CD & GITOPS (additional questions)
  // =============================================
  {
    id: 'cicd-011',
    category: 'cicd-gitops',
    question: 'What is the purpose of ArgoCD\'s `ignoreDifferences` field and when do you need it?',
    options: [
      'It tells ArgoCD to skip health checks for specified resources during sync',
      'It instructs ArgoCD to exclude specified fields from diff calculation — prevents perpetual OutOfSync when controllers mutate fields after deployment (e.g., HPA modifying `spec.replicas`, sidecars injecting annotations)',
      'It disables self-healing for specified resources so manual edits persist',
      'It marks certain resources as optional so sync succeeds even if those resources fail to apply',
    ],
    correctIndex: 1,
    answer: '`ignoreDifferences` specifies JSON pointers or JQ paths on specific resource kinds that ArgoCD excludes from its diff. This is needed when Kubernetes controllers or mutating webhooks change fields after ArgoCD deploys them — without it, ArgoCD would show perpetual OutOfSync. Common cases: HPA overriding `spec.replicas`, service mesh injecting sidecar annotations, cert-manager updating certificate status fields. It does not affect health checks, self-heal behavior, or optional resources.',
    hint: 'What happens to a resource after ArgoCD deploys it if a controller changes a field ArgoCD also manages?',
    difficulty: 'normal',
    tags: ['argocd', 'sync', 'ignoredifferences', 'drift'],
  },
  {
    id: 'cicd-012',
    category: 'cicd-gitops',
    question: 'What is the difference between `workflow_dispatch` and `repository_dispatch` triggers in GitHub Actions?',
    options: [
      '`workflow_dispatch` runs on push to any branch; `repository_dispatch` runs only on the default branch',
      '`workflow_dispatch` is triggered manually via the GitHub UI or API with typed inputs; `repository_dispatch` is triggered via POST to the GitHub API with a custom event type — designed for external system integration',
      '`workflow_dispatch` supports matrix builds; `repository_dispatch` does not',
      'They are identical; `repository_dispatch` is the deprecated name for `workflow_dispatch`',
    ],
    correctIndex: 1,
    answer: '`workflow_dispatch` enables manual triggering — via the GitHub Actions UI "Run workflow" button or API, with typed inputs defined in the workflow. `repository_dispatch` is for machine-to-machine events: an external system POSTs to `/repos/{owner}/{repo}/dispatches` with a `client_payload` and `event_type`, and workflows listening for that event type are triggered. Use `repository_dispatch` when an external system (deployment platform, monitoring tool) needs to trigger a workflow.',
    hint: 'One is for humans and simple API calls; the other is for machine-to-machine event integration.',
    difficulty: 'easy',
    tags: ['github-actions', 'triggers', 'workflow-dispatch', 'automation'],
  },
  {
    id: 'cicd-013',
    category: 'cicd-gitops',
    question: 'A developer patches a Deployment replica count directly with `kubectl`. ArgoCD self-heal is enabled. What happens?',
    options: [
      'ArgoCD detects the drift and records a warning but does not revert — replica count is assumed to be HPA-managed',
      'ArgoCD detects the live state differs from Git, marks the Application OutOfSync, and automatically syncs to revert to the Git-defined replica count',
      'ArgoCD detects the drift and creates a Git commit reverting the change',
      'Nothing happens until the next scheduled sync interval',
    ],
    correctIndex: 1,
    answer: 'With self-heal enabled, ArgoCD continuously watches live cluster state against Git. When it detects a difference, it automatically triggers a sync to restore the cluster to the Git-defined state — reverting the manual kubectl change. ArgoCD does not write back to Git; it only reconciles cluster → Git direction. The revert is fast because ArgoCD uses Kubernetes watch events, not polling. To allow HPA to manage replicas without this conflict, use `ignoreDifferences` on `spec.replicas`.',
    hint: 'Self-heal means ArgoCD enforces Git as the source of truth. Which direction does reconciliation flow?',
    difficulty: 'normal',
    tags: ['argocd', 'self-heal', 'gitops', 'drift'],
  },
  {
    id: 'cicd-014',
    category: 'cicd-gitops',
    question: 'Flux CD `ImageUpdateAutomation` is configured but not committing updated image tags to Git. What should you check first?',
    options: [
      'Whether the Flux source-controller has network access to the container registry',
      'Whether the `ImagePolicy` has a `latestImage` status populated AND whether the GitRepository write credentials have push permissions to the repo',
      'Whether the HelmRelease has `spec.upgrade.force: true` set',
      'Whether the `ImageRepository` scan interval is shorter than the `ImageUpdateAutomation` interval',
    ],
    correctIndex: 1,
    answer: 'Image automation requires two things: (1) the `ImagePolicy` must have successfully resolved a `latestImage` — check `kubectl get imagepolicy -o yaml` for the status field; (2) the `GitRepository` source must be configured with credentials that have push (write) access, not just pull. Read-only deploy keys are a common misconfiguration. HelmRelease force upgrade is unrelated. Scan interval timing can cause delays but will not prevent commits entirely once a new image is found.',
    hint: 'Flux reads from container registries and writes to Git — check both ends of that pipeline.',
    difficulty: 'hard',
    tags: ['flux', 'image-automation', 'gitops', 'troubleshooting'],
  },
  {
    id: 'cicd-015',
    category: 'cicd-gitops',
    question: 'You implement dev → staging → production promotion across separate Git repos with ArgoCD. What is the main operational challenge vs. a monorepo model?',
    options: [
      'ArgoCD cannot manage applications across multiple Git repositories',
      'Multi-repo requires separate ArgoCD instances per environment',
      'Promotion events generate commits in separate repos — answering "what config is running in production?" and correlating incidents to specific commits becomes harder; rollback requires coordinating across repos',
      'App of Apps pattern only works with a single Git repository',
    ],
    correctIndex: 2,
    answer: 'ArgoCD handles multiple repos fine and does not require separate instances per environment. The real challenge is operational: each promotion is a separate commit in a separate repo, making audit trails fragmented. Answering "exactly what is running in production?" requires correlating multiple repos. Rollback requires coordinating changes across repos. Incident-to-commit correlation is harder. Monorepos simplify these at the cost of a larger blast radius per PR. Both approaches are valid with different tradeoffs — the multi-repo model requires better tooling to maintain visibility.',
    hint: 'Think about audit trails, rollback, and answering "what exactly is running in production right now?"',
    difficulty: 'expert',
    tags: ['gitops', 'multi-repo', 'promotion', 'argocd'],
  },

  // =============================================
  // ARCHITECTURE (additional questions)
  // =============================================
  {
    id: 'arch-011',
    category: 'architecture',
    question: 'How does the bulkhead pattern differ from a circuit breaker in a microservices architecture?',
    options: [
      'Bulkheads and circuit breakers are the same pattern implemented at different layers',
      'A circuit breaker stops calls to a failing downstream service; a bulkhead isolates resource pools (thread pools, connection pools) so a slow service cannot exhaust resources shared with other dependencies',
      'Bulkheads are static configuration set at deploy time; circuit breakers are dynamic and open/close at runtime',
      'Circuit breakers apply to synchronous calls only; bulkheads apply to async message queues only',
    ],
    correctIndex: 1,
    answer: 'A circuit breaker detects failure rates and temporarily stops calls to a struggling dependency. A bulkhead isolates resource pools — if Service A has its own thread pool separate from Service B\'s, a slow Service A cannot exhaust all threads and starve calls to Service B. The name comes from ship compartments: flooding one compartment does not sink the ship. Both patterns are complementary: circuit breakers stop calls to known-bad services; bulkheads limit blast radius when a service degrades slowly without tripping the breaker.',
    hint: 'Both prevent cascading failures but at different stages — one stops the call, the other limits collateral resource consumption.',
    difficulty: 'normal',
    tags: ['resilience', 'bulkhead', 'circuit-breaker', 'microservices'],
  },
  {
    id: 'arch-012',
    category: 'architecture',
    question: 'You must process 1 million events per second with p99 latency under 100ms. A relational database is in the synchronous write path. Which change has the highest impact?',
    options: [
      'Scale up the RDS instance to the largest available instance type',
      'Move the synchronous DB write off the critical path — write to a fast message queue (Kafka/SQS) and process DB persistence asynchronously, accepting eventual consistency',
      'Add a read replica to distribute load across multiple database nodes',
      'Enable PgBouncer connection pooling to reduce per-request connection overhead',
    ],
    correctIndex: 1,
    answer: 'At 1M events/sec, even a very fast relational DB cannot handle synchronous writes while maintaining sub-100ms p99 latency. Moving DB persistence async (write to Kafka, acknowledge to client immediately, process to DB asynchronously) removes the DB from the latency critical path entirely — at the cost of eventual consistency. Vertical scaling hits hard limits fast and changes nothing fundamental. Read replicas help read throughput, not write throughput. Connection pooling reduces overhead but does not solve write serialization at this scale.',
    hint: 'At extreme write scale, the fastest database is sometimes the one not in the critical path at all.',
    difficulty: 'hard',
    tags: ['high-throughput', 'async', 'message-queue', 'latency'],
  },
  {
    id: 'arch-013',
    category: 'architecture',
    question: 'What problem does CQRS (Command Query Responsibility Segregation) primarily solve?',
    options: [
      'It separates authentication from authorization to simplify access control logic',
      'It uses separate models and datastores for writes (commands) and reads (queries) — solving the impedance mismatch between write-optimized schemas and read-optimized projections at scale',
      'It is a caching pattern where all reads go to a cache and all writes bypass the cache',
      'It is the same as read replicas — writes go to primary, reads go to replicas',
    ],
    correctIndex: 1,
    answer: 'CQRS separates the write model (commands that change state) from the read model (queries returning data). The write model uses a normalized transactional schema optimized for integrity; the read model uses denormalized projections optimized for specific query patterns. This solves the impedance mismatch where a single schema serves neither writes nor reads well. Unlike read replicas, CQRS allows the read model to have a fundamentally different data structure — not just a copy of the write model. Often paired with Event Sourcing.',
    hint: 'Why might the optimal schema for writing a transaction be different from the schema optimal for rendering a dashboard?',
    difficulty: 'normal',
    tags: ['cqrs', 'data-modeling', 'scalability', 'event-sourcing'],
  },
  {
    id: 'arch-014',
    category: 'architecture',
    question: 'You choose multi-region active-active for a write-heavy system. What is the primary consistency challenge you must explicitly design for?',
    options: [
      'All writes must synchronously replicate to all regions before acknowledging — this increases latency proportionally to inter-region round-trip time',
      'Concurrent writes to the same record from different regions create conflicts. You must choose a resolution strategy: last-write-wins, CRDTs, application-level merging, or routing writes for the same entity to the same home region.',
      'Active-active cannot use relational databases — you must migrate to NoSQL',
      'A global load balancer adds at least 200ms overhead to every request',
    ],
    correctIndex: 1,
    answer: 'In active-active, any region can accept writes for any record. Two users in different regions can simultaneously update the same entity — without a strategy, one write silently overwrites the other. Solutions: route entity-specific writes to a "home region" (avoiding the conflict), use CRDTs for data structures that can be merged commutatively, last-write-wins (simple but lossy), or application-level conflict detection. Synchronous replication is the active-passive (strongly consistent) trade-off, not the defining challenge of active-active.',
    hint: 'What happens when two regions accept a write for the exact same data record simultaneously?',
    difficulty: 'hard',
    tags: ['multi-region', 'active-active', 'consistency', 'conflict-resolution'],
  },
  {
    id: 'arch-015',
    category: 'architecture',
    question: 'A new feature must display data from 5 separate microservices. Calling all 5 synchronously on every request is too slow (300ms+). What patterns best address this?',
    options: [
      'Merge all 5 services into one to eliminate network overhead',
      'API Composition pattern for simple aggregations, or a CQRS read model (materialized view updated via events) for high-read-throughput — both avoid synchronous fan-out on the critical path',
      'Add GraphQL in front of all 5 services and rely on DataLoader batching',
      'Increase the request timeout to 2000ms to accommodate the aggregation',
    ],
    correctIndex: 1,
    answer: 'API Composition (an aggregator calls dependencies in parallel and merges) works for simple cases with moderate traffic. For high read throughput, a dedicated read model subscribes to domain events from all 5 services and maintains a pre-computed queryable projection — paying the aggregation cost at write time, not read time. GraphQL with DataLoader helps with N+1 problems but does not eliminate 5 network round trips. Merging services eliminates the network cost but reintroduces coupling. Longer timeouts make the problem worse under load.',
    hint: 'Pay the aggregation cost at read time (slow, scales poorly) or at write time (fast reads, eventual consistency)?',
    difficulty: 'expert',
    tags: ['api-composition', 'cqrs', 'read-model', 'microservices'],
  },

  // =============================================
  // OBSERVABILITY (additional questions)
  // =============================================
  {
    id: 'obs-006',
    category: 'observability',
    question: 'What is the difference between metric "resolution" and "retention" in a time-series database, and how does Thanos address both?',
    options: [
      'Resolution is decimal precision of metric values; retention is how long raw values are kept',
      'Resolution is the interval between data points (scrape granularity); retention is how long data is stored. Thanos downsamples older data to coarser resolution to reduce storage while extending retention.',
      'Resolution and retention are synonymous in Prometheus; Thanos introduces the distinction by adding tiered storage',
      'Resolution is the number of time series stored; retention is the per-series storage cap',
    ],
    correctIndex: 1,
    answer: 'Resolution (granularity) is the time interval between samples — finer resolution (15s scrapes) means more data points per hour. Retention is how long that data is stored. Thanos addresses long-term storage by downsampling: old raw data (15s resolution) is aggregated into coarser samples (5min, 1hr) and raw data is discarded, dramatically reducing storage costs while preserving long-term trend data. Thanos Compactor handles this automatically based on configured thresholds.',
    hint: 'Think of resolution as the density of data points and retention as their lifespan.',
    difficulty: 'easy',
    tags: ['prometheus', 'thanos', 'retention', 'downsampling'],
  },
  {
    id: 'obs-007',
    category: 'observability',
    question: 'Prometheus scrape durations are increasing and some scrapes are timing out for a service exposing 500K unique time series. What is the most effective mitigation without changing the application?',
    options: [
      'Increase the Prometheus scrape timeout from 10s to 60s in the scrape config',
      'Shard the scrape across multiple Prometheus instances (each scraping a pod subset) and aggregate with Thanos or federation — distributes both scrape load and TSDB pressure',
      'Enable remote write to offload TSDB storage pressure to a remote backend',
      'Switch from pull-based scraping to push-based metrics using Pushgateway',
    ],
    correctIndex: 1,
    answer: 'With 500K series from a single service, one Prometheus instance struggles to scrape, store, and query this volume. Sharding the scrape — each Prometheus instance scrapes a subset of pods — distributes both the network load and TSDB write pressure. Thanos Sidecar or federation then aggregates across shards for unified querying. Increasing timeout treats the symptom, not the cause. Remote write helps with durability but does not reduce the per-instance scrape load. Pushgateway is for short-lived jobs and does not solve scalability.',
    hint: 'The problem is load concentration on a single Prometheus instance — think horizontal distribution.',
    difficulty: 'hard',
    tags: ['prometheus', 'cardinality', 'federation', 'scalability'],
  },
  {
    id: 'obs-008',
    category: 'observability',
    question: 'What is a Prometheus "exemplar" and why is it valuable for observability?',
    options: [
      'An exemplar is a golden signal metric considered the best example for alerting on a given service',
      'An exemplar is metadata attached to a metric data point linking it to a specific trace ID — enabling a direct jump from an anomalous metric spike to the distributed trace that caused it',
      'An exemplar is a pre-computed recording rule used as a reference baseline for anomaly detection',
      'An exemplar is a community-curated alert rule from the Prometheus Alerting Rules library',
    ],
    correctIndex: 1,
    answer: 'Exemplars are a feature of the OpenMetrics format (Prometheus 2.26+) that attach a trace ID to specific metric data points. When you see a latency spike in Grafana, exemplars let you click that data point and jump directly to the Jaeger/Tempo trace for the specific request that caused it. This bridges the gap between metrics (aggregated) and traces (individual request detail) — one of the key integrations of the three pillars of observability. Requires both the app to emit exemplars and Grafana Tempo/Jaeger as the trace backend.',
    hint: 'This feature creates a direct link between two of the three observability pillars.',
    difficulty: 'normal',
    tags: ['exemplars', 'tracing', 'prometheus', 'openmetrics'],
  },
  {
    id: 'obs-009',
    category: 'observability',
    question: 'Log volume is 10TB/day and ingestion costs are significant. Which strategy best reduces cost without impairing incident response?',
    options: [
      'Switch from structured JSON to plaintext to reduce per-log byte size',
      'Sample high-volume INFO/DEBUG logs at the collector level (OpenTelemetry Collector) while forwarding 100% of ERROR and WARN — preserves signal, cuts volume 80-90%',
      'Increase log verbosity and rely on compression — compressed JSON is smaller than compressed plaintext',
      'Reduce metrics scrape interval from 15s to 60s to compensate for log storage costs',
    ],
    correctIndex: 1,
    answer: 'Head-based log sampling at the collector tier (e.g., OTel Collector probabilistic sampler or filter processor) keeps all high-severity logs while sampling noisy INFO/DEBUG traffic. This typically reduces volume 80-90% with minimal incident response impact since errors are fully preserved. Switching to plaintext loses queryability and saves little space after compression. Higher verbosity produces more logs — worse for cost. Metrics scrape interval has no relation to log storage costs.',
    hint: 'Not all log lines have equal operational value — design sampling around severity.',
    difficulty: 'normal',
    tags: ['logging', 'cost-optimization', 'sampling', 'opentelemetry'],
  },
  {
    id: 'obs-010',
    category: 'observability',
    question: 'Your alerting fires when error rate > 1% for 5 minutes. Your SLO is 99.9% (43.8 min error budget/month). What is the fundamental flaw in this alerting strategy?',
    options: [
      'The threshold (1%) is too high — it should be 0.1% to match the SLO',
      'The alert is not budget-aware: it fires on instantaneous rate regardless of budget burn speed. A 0.5% error rate sustained for hours silently exhausts the budget; a 2% spike for 3 minutes fires the alert but barely dents it.',
      'The 5-minute window is too short — extend to 1 hour to reduce alert fatigue',
      'SLOs should always be based on latency, not error rate',
    ],
    correctIndex: 1,
    answer: 'Threshold-based alerting on instantaneous error rates is not aligned with error budget consumption. A slow-burn incident at 0.5% errors for 24 hours can consume the entire monthly budget without ever tripping the alert. A 2% spike for 3 minutes fires the alert but barely affects the budget. SLO-based alerting (burn rate alerts) instead asks: "how fast are we consuming the error budget?" The Google SRE Workbook formalizes multi-window, multi-burn-rate alerting: fast window detects acute incidents, slow window catches slow burns. Both are needed.',
    hint: 'Consider two scenarios: tiny sustained error rate vs. large short spike. Which actually threatens the SLO?',
    difficulty: 'expert',
    tags: ['slo', 'error-budget', 'burn-rate', 'alerting'],
  },
];
